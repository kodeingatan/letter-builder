# LBS (Letter Builder System) local environment — copy to `.env` for local overrides (never commit `.env`).
# Production requires a strong JWT_SECRET; dev boots (no env vars,
# synchronize:true) are startup-warn-free by design (see docs/production-runbook.md §1).
JWT_SECRET=
DB_SYNCHRONIZE=
# NODE_ENV=production
