# Audit screenshots — Task 25 placeholder only; binaries ignored (verify suggestion)
docs/audit/screenshots/*.png
docs/audit/screenshots/*.jpg
docs/audit/screenshots/*.webp
!docs/audit/screenshots/README.md
!docs/audit/screenshots/.gitkeep

# Windows artifact — Task 02: stray `nul` from `2>nul` redirect (use `2>nul:` or `2>$null` in PowerShell)
nul
