## Acceptance Criteria

> MANDATORY — Given/When/Then. Setiap AC mapping ke `User Flow` Step + `Requirements` (BR/EC) + FASE 1 `AC-D01..08`. Kriteria dedicated pages + Tiptap guard + DR-002 polish + video menjadi gating `DONE`.

### AC-001 — Dedicated create page Master Data ber-layout sama & navigasi

Given Super Admin di `/dashboard/master-data` list (PageShell + DataTable kanonis `Cari...` `Semua Kolom` + NButton pill `+ Buat Tabel` `h(NIcon Add)`)

When klik `+ Buat Tabel` → `router.push('/dashboard/master-data/create')` (`<a href="/dashboard/master-data/create" onClick preventDefault router.push>` preserve right-click/Ctrl+click new tab) → PageShell breadcrumb `Dashboard / Master Data / Buat` title `Buat Tabel` 20px Semibold tracking -0.125px `description 12px #615d59` + card `bg-[#ffffff] border hairline rounded-[12px] p-6 Level-1` + `NDynamicInput` kolom + `ColumnConfigPanel` + footer sticky `Batal | Simpan` (Batal → `router.back()`, Simpan → `POST /api/master-data` `CreateMasterTableSchema` `name/display_name/slug/columns 13 tipe`) → submit valid

Then `201`, `mst_pegawai` tercipta, toast `Berhasil` (ID) `useMessage`, redirect `navigateTo('/dashboard/master-data')` list re-fetch, menu `Master Data → Pegawai` muncul; **edit via `/dashboard/master-data/:slug/edit` layout identik** (title `Edit: Pegawai v2` pre-filled `GET /api/master-data/:slug` + `PUT` vs `POST`); 400/409 → `NFormItem validationStatus="error"` + `NAlert` summary + focus first error; no `NModal` create lama sebagai jalur utama.

> Trace: `Flow Step 1-4` → `Flow→UI Master Data dedicated` → `Flow→API #1 POST /api/master-data` → `FR-001` `BR-001` `BR-009` → `10/AC-D01` → `Tasks Frontend dedicated page`

### AC-002 — Dedicated create & edit Component dengan Tiptap hardened (fix `configure` crash)

Given Super Admin di `/dashboard/components` list → klik `+ Buat Component` → `navigateTo('/dashboard/components/create')` (DEDICATED PAGE baru, layout sama via PageShell `title Buat Component` + `breadcrumbs [Dashboard, Component, Buat]`)

When isi `name=Kop Surat`, toggle `is_looping=false`, Tiptap office-min toolbar grouped visible: `B/I/U/S` + Heading `H2` `NSelect` + Align `Center` + List `•` + Insert `Table/Image` + `+ Binding` fallback 44px; ketik "Kepala Dinas" → `Bold` + `Heading 2` + `Align center` → right-click → `BindingPalette` (`name=kop.nama, view=text, target=pegawai.nama` master_data) → insert pill `{{kop.nama}}` `bg-[#e8f2fd]`; Save → `POST /api/doc-components` `CreateDocComponentSchema`

Then `201` `Kop Surat v1` (preview_html cache), redirect list; **edit via `/dashboard/components/7/edit`** (dedicated PageShell `title Edit: Kop Surat v1` + breadcrumb `Dashboard / Component / Edit` **layout identik dengan create** same padding `head 16×20 body 24 radius 12`, pre-filled `GET /api/doc-components/:id` `tiptap_json` → `editor.commands.setContent` guard `JSON.stringify` diff) → ubah `name=Kop Baru` + toolbar `Underline` + Save → `PUT /api/doc-components/:id` → version `2` toast `Component diperbarui (versi naik)` ; **Tiptap tidak throw `Cannot read properties of undefined (reading 'configure')`** pada create & delete — verified `vue-tsc` 0 + `npm run build` + E2E `letter-builder-polish-component.spec` `configureError` guard `try/catch NAlert+Retry` + `onBeforeUnmount editor?.destroy()` safe + `ClientOnly` fallback; delete via list `NPopconfirm` → `DELETE` 409 bila dipakai → `ReferenceList` modal `data-testid=conflict-references` (reuse 09) bukan `Server Error`.

> Trace: `Flow Step 8-11` → `Flow→UI Component dedicated` → `Flow→API #5 POST /api/doc-components` `#6 PUT/DELETE` → `FR-002` `FR-005` `FR-008` `BR-003/008/010` `ERR-02` → `10/AC-D02` `10/AC-D06`

### AC-003 — Builder `/dashboard/templates/:id` polish lebih cantik & mudah + dedicated create template

Given Admin di `/dashboard/templates` list → klik `+ Buat Template` → dedicated page `navigateTo('/dashboard/templates/create')` PageShell meta `name SK Pengangkatan` + `code sk-pengangkatan [a-z0-9-_]{2,60}` + `description` + validation inline → `POST /api/doc-templates` → redirect `/dashboard/templates/:id` Builder

When di Builder polish 3-pane grid `260|1fr|320` desktop (design 10 `builder-3pane.svg`): Library kiri `bg-[#f6f5f4]` soft `NTree` searchable filter `NInput` 32px `Cari component...` + drag handle `h(NIcon Add)` + `NTag` version; Canvas tengah `bg-[#f6f5f4]` `p-4` flex-1 + `DocumentCanvas` card `bg-[#ffffff] hairline Level-1 rounded-[12px] p-4 min-h-[400px]` + `EmptyStateCard` illustration warm `xl16` `p-32` `#f6f5f4` + CTA `+ Tambah Blok` pill primary `#0075de` full — drag `Kop` + `Daftar` dari Library → Canvas ghost `opacity 0.5` + ring primary `border-[#0075de] bg-[#e8f2fd]` + drop; Properties kanan `NForm` live sectioned eyebrow `11px uppercase tracking-[0.05em] #94a3b8 font-semibold` + `RepeaterEditor` `Pilih semua` indeterminate + `ConditionEditor` + `DataBindingEditor`; keyboard `ArrowUp/Down` reorder `aria-grabbed` + `Enter` edit; preview drawer 600px tabs `HTML/PDF` `NCode` + `NScrollbar` + `NAlert warning`

Then Canvas menampilkan `Kop` + `Daftar` blocks live, selection ring `#0075de`, EmptyState hilang saat ada blok, dapat `Publish (v+1)` → `PUT /api/doc-templates/:id status PUBLISHED` `BR-002` valid → version 2 toast; **edit meta via `/dashboard/templates/:id/edit` dedicated PageShell same layout as create** (`Edit: SK Pengangkatan` pre-filled, not builder) → `PUT` name/code; responsive tablet drawer 260/320 + mobile `NTabs Library/Canvas/Properties` + toolbar column; `prefers-reduced-motion` reduce 0.01ms.

> Trace: `Flow Step 13-18` → `Flow→UI Template dedicated + Builder polish` → `Flow→API #7 POST /api/doc-templates` `#8 PUT` + `#9 preview-pdf` → `FR-003` `FR-006` `FR-008` → `10/AC-D03`

### AC-004 — Administrasi Mapping `DR-002` per-field editor jelas `missing: field`

Given Admin di `/dashboard/administrations` list → klik `+ Buat Administrasi` → dedicated page `navigateTo('/dashboard/administrations/create')` PageShell `title Buat Administrasi` + breadcrumb `Dashboard / Administrasi / Buat` **layout same as edit** via `PageShell` + card `p-6 radius 12 hairline Level-1`

When isi `name=SK Pengangkatan`, `slug=sk-pengangkatan`, add Step 1: `NSelect` template `SK Pengangkatan` searchable filterable → per-requirement rows auto-generate via `GET /api/doc-templates/:id` or `scanRequirements(schema)` (requirements list `letter.number, letter.tanggal, pegawai.selected`) → rows: `field NTag badge eyebrow` + `kind NSelect (value/master_data/system)` + `ref NInput/NSelect` (e.g., `letter.number` → `kind:value ref:"800/1"`, `pegawai.selected` → `kind:master_data ref:pegawai:pegawai`) → kosongkan `letter.tanggal` → klik `Simpan` → `POST /api/administrations` `{ name, slug, steps: [{template_id, step_order:0, mapping: {letter.number:{kind:"value",ref:"800/1"}}}] }` missing `letter.tanggal` → server `400` `createError({ statusCode:400, message:"Mapping incomplete — missing: letter.tanggal (DR-002)", data:{ missingFields:["letter.tanggal"], missing:"letter.tanggal" } })`

Then **tidak tampil textarea JSON mentah membingungkan**, melainkan: `NFormItem` `letter.tanggal` `validationStatus="error"` border merah `#EF4444` + `feedback "Missing: letter.tanggal (DR-002) — pilih target"` + `aria-invalid="true"` + `aria-describedby`, `NAlert type="warning" Mapping incomplete — missing: letter.tanggal` summary list `data.missingFields` di atas form + auto `ref.focus()` first missing + `scrollIntoView({ behavior:'smooth', block:'center' })` + tooltip `"Pilih data master/manual/system untuk field ini"`; fill `letter.tanggal → {kind:"system", ref:"current_date"}` → resubmit → `201` Administrasi tersimpan + redirect list + menu `[SK Pengangkatan]` muncul; edit via `/dashboard/administrations/:id/edit` **layout same** pre-filled `GET /api/administrations/:id` steps mapping + per-field highlight; E2E `letter-builder-polish-administration.spec` video merekam `missing` → `fill` → `success`.

> Trace: `Flow Step 19-22` → `Flow→UI Administration dedicated + StepMappingEditor per-field` → `Flow→API #10 POST /api/administrations` `400 DR-002 data.missingFields` → `FR-004` `FR-007` `DR-002` `BR-004` `ERR-06` → `10/AC-D04`

### AC-005 — Global UI/UX polish seragam mudah dipakai

Given semua halaman polish dedicated + Builder + Wizard + States

When audit visual token vs `app/utils/naiveui-theme.ts` (`primary #0075de/#0069c4/#005bab`, `body #f6f5f4`, `card #FFFFFF`, `border #e6e6e6`, `text #000000`, `radius 8/4 12`, `font Inter` + tracking Display −1px H3 −0.125px Eyebrow +0.125px, spacing xxs4→xxl32 via Tailwind `gap-3 p-4 bg-[#f6f5f4] border-[#e6e6e6] rounded-[12px]`, elevation Level1 `rgba(0,0,0,0.04) 0 4px 18px` + hairline, Level2 `0 23px 52px`, icon `h(NIcon)` Carbon, `NEmpty` `Belum ada data` + CTA pill `+ Buat ...` no dead-end via `NDataTable #empty` single, `NAlert` success/warning/error seragam, `NTag` badge pill `rounded-full`, `PageShell` head `16×20` body `24` radius `12` + breadcrumb `<a href>`, sidebar 220/72 active `#e8f2fd` border `#0075de`, `prefers-reduced-motion` reduce 0.01ms

Then semua halaman surat konsisten Notion-calm (warm paper + hairline + micro-shadow + pill CTA primary), tidak ada hard-coded color di luar token, no `NDescriptions` (`.detail-view` + `NForm` live), Tailwind inline utilities no over-`@apply` + `<style scoped>` only for `:deep()` override, Storybook `LetterBuilderPolish/*` 10 + `LetterBuilder/*` 08 + `foundation/*` 3 files regression PASS `npm run build-storybook` no error, `vue-tsc` 0, `npm run build` sukses ~20MB.

> Trace: `Flow Step 23` + `ALT-01..07` + `ERR-01..12` + `FR-010` → `10/AC-D05`

### AC-006 — Tiptap office-minimum toolbar very minimum tetapi lengkap

Given `ComponentEditor` di dedicated `create` + `edit` pages + `TiptapToolbar.vue` baru + referensi `https://tiptap.dev/docs/examples` (StarterKit, Placeholder, Highlight, Color/TextAlign, Underline, Link, Image, Table, BubbleMenu, FloatingMenu)

When inspeksi toolbar 1 baris grouped `flex flex-wrap gap-1` `role="toolbar"` `aria-label="Toolbar editor"` + `gap-1`: Group Text `B Bold / I Italic / U Underline / S Strike` + Heading `H1/H2/H3` `NSelect` (toggleHeading) + Align `Left/Center/Right/Justify` (`TextAlign.configure({ types:['heading','paragraph'] })`) + List `• List / 1. List` (Bullet/Ordered) + Insert `Table (2×2) / Image (prompt URL allowlist https /api/storage/ data:image/ ) / Link (prompt href)` + History `↶ Undo / ↷ Redo` (`chain.undo/redo`) + `+ Binding (klik kanan)` fallback 44px hit (`showBinding`) ; selection `BubbleMenu` (Bold/Italic/Link) + empty paragraph `FloatingMenu` (`+ Type / Heading / List`); Placeholder `Tulis konten surat...` `extension-placeholder` faint `#a39e98`; binding pill `{{kop.nama}}` atom non-editable

Then toolbar lengkap layaknya office doc tetapi **very minimum** (1 baris, tidak overload font-size/family, tidak collaboration), setiap button `runAction` via `editor.chain().focus().toggleBold()/setTextAlign()/insertTable()/setImage()` etc. `https://tiptap.dev/docs/examples` examples ter-referensi di `domain-api-ui.md` Library rationale, direct import Naive UI `NButton` small + `NIcon` Carbon, Tailwind `flex gap-1`, `isActive` state `bg-[#e8f2fd] border-[#0075de]` visible, 44px hit mobile via `+ Binding` bottom fixed, `ClientOnly` SSR safe + `configure` guard no crash; Storybook `TiptapToolbar` + `ComponentEditor` variants pass a11y, E2E `letter-builder-polish-component.spec` clicks each group.

> Trace: `Flow Step 9` → `Flow→UI TiptapToolbar` → `FR-005` `FR-008` → `10/AC-D06` `https://tiptap.dev/docs/examples`

### AC-007 — Playwright video `https://playwright.dev/docs/videos#record-video` untuk setiap pengujian

Given `apps/web/playwright.config.ts` hardened + `https://playwright.dev/docs/videos#record-video` (fitur `video`, `trace`, `screenshot`, `expect`, `contextOptions` size) + `npm run test:e2e` HEADLESS=1 `reuseExistingServer: true` `:3000` chromium only + `test/e2e/letter-builder-polish-*.spec.ts` 6 files (E2E-01..06 polish) + existing `letter-builder-*.spec.ts` 6 + `crud.spec` etc.

When `playwright.config.ts` `use: { video: { mode: 'on', size: { width:1280, height:720 } }, trace: 'on-first-retry', screenshot: 'only-on-failure', expect: { timeout: 5000 } }` + `headed = HEADLESS !== '1' && CI !== 'true'` + `slowMo = headed ? 100 : 0` + `viewport 1280×720` + `webServer npx nuxi dev --port 3000` `reuseExistingServer: true` `timeout 120000` → jalankan `HEADLESS=1 npm run test:e2e -- letter-builder-polish` → artifacts `test-results/**/video.webm` + `playwright-report/data` + `trace.zip` (on retry) dapat di `npx playwright show-report` atau `playwright show-trace`

Then **setiap pengujian merekam `video.webm`** bahkan `HEADLESS=1` (kecuali CI `CI=true` bila fallback `retain-on-failure` disk limit documented), HEADLESS vs headed transition proven (`HEADLESS=1` → `headless true slowMo 0` + video `on` retains), video size 1280×720 sesuai viewport, trace on-first-retry tidak overlap video; E2E-01 master dedicated, E2E-02 component Tiptap + 409 ReferenceList, E2E-03 template Builder polish, E2E-04 mapping DR-002 per-field, E2E-05 wizard, E2E-06 errors/permission/edge/mobile + video artifact assert `expect.soft(videoPath).toBeTruthy()` atau `testInfo.attach`; config tidak break existing `letter-builder-*.spec.ts` 409 modal not Server Error; doc ref `https://playwright.dev/docs/videos#record-video` di `verification.md` manual checklist.

> Trace: `Flow Step 25` → `FR-009` → `10/AC-D07`

### AC-008 — Traceability & regression & build tidak pecah

Given `playwright.config.ts` video `on` + dedicated pages routing + Tiptap guard + Builder polish + DR-002 per-field + global polish + Storybook 10 + existing 08 9 stories + `vue-tsc` + `build` + `build-storybook`

When audit `Flow→UI/API Mapping` + `Test Plan` `UT-01..04 NT-01..06 E2E-01..06 polish` + `grep -rn "video.*on" playwright.config.ts` 1 hit `video: { mode: 'on', size` + `grep addEventListener rbac-denied` 1 hit `AccessDeniedAlert` single + `grep configure` guard `mod.default ?? mod` + `data-testid=conflict-references` 4 hits + `StepMappingEditor` import `missingFields` + `vue-tsc --noEmit` 0 + `npm run build` ~21MB + `npm run build-storybook` Vite no error `LetterBuilderPolish` chunks + `npm run test:unit` + `test:nuxt` + `HEADLESS=1 npm run test:e2e` video artifacts

Then coverage `Flow Steps 24/24 (100%)` + `AC-001..008 8/8 (100%)` + `BR 10/10 (100%)` + `EC 10/10 (100%)` + `ERR-01..12 (100%)` (incl. DR-002 `missing: field` per-field); no `Server Error` raw untuk 409/400/404/500; `Storybook` regression `LetterBuilderPolish 7` + `LetterBuilder 9` + `foundation 3` PASS; `video.webm` per-test exists `test-results/`; traceability `User Flow ↔ AC ↔ Test ID` 100% tabel `Test Plan` di verification.

> Trace: `Cross-Cutting` + `10/AC-D08` → `Tasks Test Plan` matrix

## Tasks

> MANDATORY — checklist implementasi. Mengacu `10-letter-builder-polish-ui-design`. Tandai `[x]` hanya bila lolos `vue-tsc` + test + video + build.

### Backend

- [ ] Verifikasi & uniform `DR-002` missing per-field `data.missingFields` — `server/api/administrations/index.post.ts` + `[id].put.ts` + `server/services/administrations.service.ts:replaceSteps` (`throw createError({ statusCode:400, message:`Mapping incomplete — missing: ${missing.join(', ')} (DR-002)`, data:{ missingFields: missing, missing: missing.join(', ') } })`) — pastikan `data.missingFields` array ter-serialisasi JSON dan tidak tertutup 500; `grep -rn "Missing.*DR-002" server` 1 hit tetap; `createError statusCode ?? 500` seragam `server/api/**`.
- [ ] Hardening no new entity — audit `server/utils/orm-data-source.ts` `appEntities 9+5` + `appMigrations Baseline+CreateMasterTables+CreatePersuratanTables` + `isMasterPhysicalTable` ignore `mst_*`; no migration baru; `synchronize: true` dev + `migrationsRun` prod remain.
- [ ] Uniform 400/404 → Zod discriminated 13 tipe (`server/dto/master-data.dto.ts:96-122`, `server/dto/persuratan.dto.ts:24-50` `CreateDocComponentSchema 1MB TiptapDocSchema` + `DocNodeSchema assertTreeLimits depth≤10 total≤200 item/level 500` + `CreateAdministrationSchema steps max20`) + slug regex `[a-z0-9_]`/`[a-z][a-z0-9-]{1,60}` + `is_looping BR-003` + publish `BR-002` + `step_order INV-002` unique + `document_number BR-006` unique — pastikan `notFound` helper 404 konsisten.
- [ ] Image allowlist & renderer: `ExpressionService.evalTextOperation` recompute server div-by-zero → `null`+400; `coerceValue` image `https://`/`/api/storage/`/`data:image/` allowlist (same as `renderer.service` sanitize-html) — unit `master-operation.test.ts` existing reuse.
- [ ] ActivityLog & seed: tetap `ActivityLogsService.log` per aksi (master delete, component, template, administration DR-002) catch silent; seeder `LetterBuilder:*` backfill tetap (stub `permission-matrix.ts` pin Task02).
- [ ] Unit tests backend — `doc-components` duplicate 409 + findReferences exact vs `configure` guard (UT-01), `doc-templates` duplicate code 409 + publish guard + steps 409 (UT-02), `master-data` duplicate slug 409 + refs 409 (UT-03), `administrations DR-002 missing per-field data.missingFields` (UT-04 + DTO), `TiptapDocSchema` 1MB + Placeholder (UT-05), `playwright.config video` no backend.

### Frontend

- [ ] **Dedicated pages routing** — buat 6 dedicated PageShell pages:
  - `app/pages/dashboard/components/create.vue` (PageShell `title Buat Component` + `breadcrumbs [Dashboard, Component, Buat]` + `ComponentEditor` office-min + `NForm` `name + is_looping` + footer `Batal | Simpan` pill `POST /api/doc-components` + `navigateTo('/dashboard/components')` + unsaved guard `onBeforeRouteLeave NDialog`)
  - `app/pages/dashboard/components/[id]/edit.vue` (same layout `title Edit: ${name} v${version}` + pre-filled `GET /api/doc-components/:id` `tiptap_json` + `PUT` + version bump toast)
  - `app/pages/dashboard/templates/create.vue` (meta `name/code/description` + validation `code` regex + `POST /api/doc-templates` → `navigateTo('/dashboard/templates/' + id)`)
  - `app/pages/dashboard/templates/[id]/edit.vue` (meta edit `PUT` same layout, not builder; builder remains `[id].vue` polish)
  - `app/pages/dashboard/administrations/create.vue` (PageShell `title Buat Administrasi` + `StepMappingEditor.vue` per-field DR-002 + `POST /api/administrations`)
  - `app/pages/dashboard/administrations/[id]/edit.vue` (same + pre-filled `GET /api/administrations/:id` steps mapping + `PUT` + `DR-002` per-field highlight)
  - Refine `app/pages/dashboard/master-data/create.vue` & `master-data/[slug]/edit.vue` to same PageShell (ensure head 16×20 body 24 radius 12 hairline level-1, footer sticky)
  - Lists `components.vue` + `templates/index.vue` + `administrations.vue` : CTA `+ Buat` now `navigateTo('/dashboard/.../create')` with `<a href>` preserve right-click (remove NModal create as main path; keep `ReferenceList` 409 modal + `RelationPicker` + `DocumentPreviewDrawer` only)
- [ ] **Tiptap hardening `app/components/features/persuratan/ComponentEditor.vue`** — audit ESM `await import('@tiptap/*')` : `const s = await import('@tiptap/starter-kit'); const StarterKit = (s as any).default ?? s` ; same for `Link, TextAlign, Table, TableRow/Header/Cell, Underline, Placeholder, Highlight, Color, TextStyle` ; guard `const linkExt = Link?.configure ? Link.configure({ openOnClick:false }) : Link` fallback ; `Editor` init wrapped `try { editor = new Editor({ element: editorHost.value, content: props.modelValue ?? {type:'doc',content:[{type:'paragraph'}]}, extensions: [StarterKit, linkExt, textAlignExt, tableExt, TableRow, TableHeader, TableCell, Underline, Placeholder?.configure ? Placeholder.configure({ placeholder:'Tulis konten surat...' }) : Placeholder, Highlight?.configure ? Highlight.configure() : Highlight, DocBinding, DocRepeater, DocCondition].filter(Boolean), editorProps:{ attributes:{ class:'tiptap-editor' }, handleDOMEvents:{ contextmenu:(_v,e)=>{e.preventDefault();showBinding=true;return true}}}, onUpdate:({editor:ed})=>emit('update:modelValue', ed.getJSON()) })} catch(e){ editorError.value=(e as Error).message }` ; `onBeforeUnmount if (editor && !editor.isDestroyed) editor.destroy()` ; `ClientOnly` `#fallback` + `NSpin Memuat editor...` + `NAlert error Gagal memuat editor (configure) + Retry` button `retryInitEditor()`.
- [ ] **Extract `app/components/features/persuratan/TiptapToolbar.vue`** — grouped 1 baris `flex flex-wrap gap-1` `role="toolbar"` `aria-label="Toolbar editor"`: `B/I/U/S` `NButton` `h(NIcon)` Carbon (Add/Edit/TextAlign) + `NSelect` H1-H3 heading (`toggleHeading {level}`) + Align `Left/Center/Right/Justify` + List `• / 1.` (`toggleBulletList/OrderedList`) + Insert `Table (insertTable 2×2) / Image (prompt src allowlist) / Link (prompt href)` + History `↶ Undo / ↷ Redo` (`chain.undo/redo`) + `+ Binding` fallback 44px `type="info"` → emit `showBinding`; active state via `editor.isActive('bold')` → `bg-[#e8f2fd] border-[#0075de]`; scroll-x mobile `overflow-auto`; very minimum office per `https://tiptap.dev/docs/examples`.
- [ ] **Administrasi per-field `app/components/features/persuratan/StepMappingEditor.vue`** — per-step card `border hairline rounded-[8px] p-3 bg-[#ffffff]` + header `Step 1: Template SK` `NTag` + `NSelect` template searchable filterable `options: store.templates.map(t=>({label:`${t.name} (v${t.version})`, value:t.id}))` + for each `field` in `requirements` (auto-fetch `GET /api/doc-templates/:id` → `template.schema_json` or `scanRequirements` helper `utils/scan-requirements.ts` reuse) render `NFormItem` per `field` (`label NTag eyebrow 11px uppercase #94a3b8` `field` `NTag` badge) + `kind NSelect (value/master_data/system)` + `ref NInput` or `NSelect` (if master_data → NSelect `master-data:pegawai:field` searchable) + `validationStatus="error"` + `feedback "Missing: letter.tanggal (DR-002) — pilih target"` via `missingFields.includes(field)` + `NAlert type="warning" Mapping incomplete — missing: ${missingFields.join(', ')}` summary top + `ref` focus first `scrollIntoView` ; submit build `mapping: Object.fromEntries(rows.map(r=>[r.field,{kind:r.kind,ref:r.ref}]))` .
- [ ] **Builder polish `app/pages/dashboard/templates/[id].vue`** — redesign warm `grid gap-4 lg:grid-cols-[260px_1fr_320px]` + library `aside.border-r hairline bg-[#f6f5f4] p-3` + `NInput` filter `Cari component...` 32px + `NTree` `ComponentLibrary.vue` searchable + canvas `bg-[#f6f5f4] soft p-4` flex-1 + `DocumentCanvas.vue` polish + `EmptyStateCard` warm `xl16 p-32` + CTA `+ Tambah Blok` pill + Properties `aside.border-l hairline p-3 overflow-auto` `NForm` live eyebrow `11px uppercase` + `RepeaterEditor` `Pilih semua` indeterminate (header checkbox `checked?all: some?indeterminate:false`) + drag ghost `opacity 0.5` ring `bg-[#e8f2fd]` `border-dashed #0075de` + keyboard `ArrowUp/Down` reorder `aria-grabbed`; preview drawer 600px `NTabs HTML/PDF` `NCode` + `NScrollbar` + preview HTML escaped sanitize; save `handleSave` `POST/PUT` + `message.success`; ensure `useBuilderStore` blocks/selectedId/update/move/add unchanged.
- [ ] **Global polish wiring** — audit `master-data/create.vue` `edit.vue` + `components/create.vue` `components/[id]/edit.vue` + `templates/create.vue` `templates/[id].vue` + `administrations/create.vue` `administrations/[id]/edit.vue` + `documents/[slug].vue` wizard + `documents/index.vue` : apply token Notion `#0075de/#f6f5f4/#e6e6e6` Inter tracking xs4/full `12`, `NEmpty` CTA pill `+ Buat ...` no dead-end, `NAlert` warning vs error konsisten, `NTag` badge pill, `PageShell` same, sidebar 220/72, `prefers-reduced-motion` reduce 0.01ms `app/assets/css/main.css`.
- [ ] **Playwright video wiring** — harden `apps/web/playwright.config.ts:use: { baseURL:'http://localhost:3000', headless: !headed, viewport:{width:1280,height:720}, video: { mode: 'on', size:{width:1280,height:720} }, trace: 'on-first-retry', screenshot: 'only-on-failure' }` + `projects:[{name:'chromium', use:{ ...devices['Desktop Chrome'] } }]` + `webServer { command:'npx nuxi dev --port 3000', port:3000, reuseExistingServer:true, timeout:120000 }` per `https://playwright.dev/docs/videos#record-video` (section `Record video` + `Record trace`); HEADLESS=1 vs CI=true headed false + slowMo 100 vs 0 preserved.
- [ ] **Persistence & a11y wiring** — `localStorage:master-data:<slug>:visibility` (min1 guard), `localStorage:letter-builder:draft:<slug>` wizard draft banner `DRAFT` `NAlert info`, `localStorage:letter-builder:draft:component|template|administration` autosave dedicated create `watchDebounced`; `DataTable` kanonis toolbar 320/160 + `Restart` `aria-label="Segarkan data"` + `Settings` + `Reset` → re-fetch without reset; `aria-label` icon-only (`Bold`, `Tambah step`), `aria-current="page"` breadcrumb leaf & `aria-current="step"` wizard, `aria-grabbed` draggable, `data-testid=access-denied` single & `data-testid=conflict-references` list `aria-modal="true"` + Tiptap `role="toolbar"`; focus trap `NModal` + `NSteps` `tabindex 0` + `Enter`.
- [ ] Unit/NT tests dedicated — `test/nuxt/letter-builder-polish-dedicated.test.ts` (dedicated create vs edit same layout, navigate via `router.push` preserve href, Tiptap configure guard not throw, DR-002 per-field highlight `missingFields`, Builder polish 3-pane), `test/nuxt/letter-builder-polish-tiptap.test.ts` (toolbar grouped, BubbleMenu/FloatingMenu, Placeholder, `+ Binding` 44px, `configure` undefined mock fallback), `test/nuxt/master-data.form.test.ts` refine (dedicated + DR-002), `test/nuxt/template-builder.test.ts` polish (drag+keyboard + EmptyStateCard).

### Cross-Cutting

- [ ] RBAC matrix & guards: backfill seed `LetterBuilder:*` reuse 05-07 (Component/Template/Administration/Document Read/Write + `Master Data Read/Write`) + guard allow `/api/master-data/*`, `/api/doc-*`, `/api/administrations/*`, `/api/documents/*`, `/dashboard/components/*` etc. — tidak tambah permission baru; verifier `requireApiAccess` `matchUrlPattern` whitelist; `isMasterPhysicalTable` drift ignore `mst_*` tetap `apps/web/server/utils/migration-status.ts` + `orm-data-source.ts` appMigrations `Baseline+CreateMasterTables+CreatePersuratanTables` orde tetap; `STORAGE_DIR` allowlist `settings/avatars/general` reuse.
- [ ] `getErrorMessage` vs `getConflict*` vs `missingFields` — pastikan `app/utils/error.ts` `getErrorMessage` + `isConflictError/getConflictReferences/getConflictData` + new `getMissingFields(e): string[] | null` handle both shapes (`e.response?.data?.data?.missingFields`, `e.data?.missingFields`, `e.response?.data?.missingFields`) agar `StepMappingEditor` tidak `undefined` (source bug DR-002 generic 500).
- [ ] Storybook regression 10 — `npm run storybook` + `npm run build-storybook` tetap PASS — `LetterBuilderPolish/*` 7 files + `LetterBuilder/*` 9 files 08 + `foundation/*` 3 files; `storybook-static/index.json` berisi `LetterBuilderPolish` + `LetterBuilder`; no `NDescriptions` (pakai `.detail-view` + `NForm` live); `vue-tsc` 0 before storybook.
- [ ] Verifikasi konsistensi `10` ↔ `11`: `User Flow` 24 steps `10` = `11` (tidak deviasi UX tanpa catatan); dedicated pages `PageShell` same layout verified `NForm` gap + `Batal | Simpan` pill; `UI > Penyesuaian dari design` di `domain-api-ui.md` jelaskan `NModal` create hidden + video `video: 'on'` dengan size `1280×720`; no redesign token.
- [ ] Docs: update `tasks/README.md` + `tasks/task-logs.md` (task 11 FASE 2, Depends on 10, Implemented [ ] → [x] after, Verified, Reviewed).

### Test Plan (QA — Bertindak sebagai QA Engineer)

> MANDATORY untuk FASE 2. Setiap User Flow step, Alternate/Error `ERR-01..12`, Business Rule `BR-001..010`, Edge Case `EC-01..10`, Video HARUS pasangan test. Dipakai `/verify` + `/review`. Semua `video: 'on'` per `https://playwright.dev/docs/videos#record-video` → `video.webm` artifact per `test-results/`.

| ID | Jenis Test | File (rencana) | Mengcover | User Flow Step / AC |
|----|------------|----------------|-----------|---------------------|
| UT-01 | Unit — DocComponents Tiptap guard | `test/unit/server/services/doc-components.service.test.ts` + `test/unit/app/utils/tiptap-nodes.test.ts` | Duplicate `name` 409 + `findReferences` exact `"componentId":"<name>"` (EC-10) + `is_looping BR-003` 400 + Tiptap node `DocBinding` parseHTML `span[data-doc-binding]` | FR-002/005, BR-003/010, AC-002 |
| UT-02 | Unit — DocTemplates publish + builder limits | `test/unit/server/services/doc-templates.service.test.ts` | Duplicate `name/code` 409 + `remove` 409 `steps/administrations` (BR-004) + publish `BR-002` 400 + version bump `BR-005` + `DocNodeSchema` depth≤10 total≤200 `assertTreeLimits` `BR-008` | FR-003/006/008, BR-002/004/005/008, AC-003 |
| UT-03 | Unit — MasterData whitelist + DR-001 | `test/unit/server/services/master-data.service.test.ts` + `test/unit/server/services/master-operation.test.ts` | Duplicate slug 409 `BR-001` + `findReferences` `target_slug` + whitelist `is_searchable/orderable` 400 `BR-006` + div-by-zero `null`+warning `BR-007` + image allowlist `https`/`/api/storage/`/`data:image/` | FR-001, BR-001/006/007, AC-001 |
| UT-04 | Unit — Administrations DR-002 per-field `missingFields` + DTO | `test/unit/server/services/administrations.service.test.ts` + `test/unit/server/dto/persuratan.dto.test.ts` | Duplicate `name/slug` 409 + `document_number` unique 409 `BR-006` + `replaceSteps` `step_order` unique 400 `INV-002` + `DR-002` `missing: field` 400 `data.missingFields` array per-field vs textarea generic + `RunWizardSchema` 1MB + `TiptapDocSchema` shallow + `DocNodeSchema` limit + `getMissingFields` helper 5-shape | FR-004/007, `DR-002` `BR-004`, INV-002, AC-004 |
| UT-05 | Unit — Renderer + sanitize | `test/unit/server/dto/documents.dto.test.ts` + `test/unit/server/services/renderer.service.test.ts` | `DocNode` depth≤10 total≤200 400 + sanitize `javascript:` → escape + allowlist `https`/`/api/storage/`/`data:image/` + `Placeholder` node sanitize | DR-005/006, BR-008, AC-002/003 |
| UT-06 | Unit — Error helper `getMissingFields` + `isConflictError` + Tiptap guard | `test/unit/app/utils/error.test.ts` + `test/unit/app/utils/tiptap.test.ts` | `getErrorMessage` fallback + `isConflictError 409` + `getConflictReferences` `data.references` + `getMissingFields` `data.missingFields` 5-shape + Tiptap guard `mod.default ?? mod` `if (!Ext?.configure) fallback` mock `undefined` → no throw | ERR-02/06/07, AC-002/004 |
| NT-01 | Nuxt — Dedicated pages same layout | `test/nuxt/letter-builder-polish-dedicated.test.ts` (new) | `PageShell` dedicated create vs edit same layout (`title` `Batal | Simpan` pill, breadcrumb `<a href>`, `aria-current="page"`), navigate `router.push` preserve `href`, `onBeforeRouteLeave` unsaved guard `NDialog`, `ComponentEditor` `ClientOnly` fallback `Memuat editor...` + `NAlert Retry` vs `configure undefined` mock not throw | Step 2/8/13/19, AC-001/002/003/004, BR-009 |
| NT-02 | Nuxt — Tiptap office-min toolbar | `test/nuxt/letter-builder-polish-tiptap.test.ts` (new) | Toolbar grouped `B/I/U/S` + Heading `NSelect` + Align + List + Table/Image/Link + History + `+ Binding` 44px fallback `data-testid=tiptap-toolbar`; `BubbleMenu` contextual + `FloatingMenu` empty + `Placeholder` `Tulis konten surat...` + binding pill `bg-[#e8f2fd]` + `configure` guard `getErrorMessage` | Step 9, FR-005/008, AC-002/006 |
| NT-03 | Nuxt — Builder polish | `test/nuxt/letter-builder-polish-builder.test.ts` (new) | Library `NTree` searchable + Canvas drag-drop HTML5 ghost `opacity 0.5` + keyboard `ArrowUp/Down` `aria-grabbed` + `EmptyStateCard` `NEmpty` + CTA `+ Tambah Blok` + Properties `NForm` live eyebrow `11px uppercase` + `RepeaterEditor` `Pilih semua` indeterminate + preview drawer 600px `NTabs` | Step 15-17, FR-006, AC-003 |
| NT-04 | Nuxt — Administration mapping DR-002 per-field | `test/nuxt/letter-builder-polish-administration.test.ts` (new) | Per-field rows `field NTag` + `kind NSelect` + `ref NInput` + `validationStatus="error"` per `missingFields` + `NAlert type="warning" Mapping incomplete — missing: letter.tanggal` summary + `focus` first + `scrollIntoView` vs textarea old `mappingText` not rendered | Step 19-20, `DR-002` `ERR-06`, AC-004 |
| NT-05 | Nuxt — DataTable kanonis + AccessDenied single | `test/nuxt/imports.test.ts` + `test/nuxt/letter-builder-polish-states.test.ts` | Toolbar kanonis 320/160 + `Restart` `aria-label="Segarkan data"` + `Settings` visibility + pagination `Menampilkan` + `rbac-denied` → single `AccessDeniedAlert` (`grep addEventListener rbac-denied` 1 hit + `data-testid=access-denied` 1) + `video` no impact + Tiptap guard no double | Step 1/5, FR-009/010, AC-005 |
| NT-06 | Nuxt — States polish (video artifact no break) | `test/nuxt/letter-builder-polish-states.test.ts` | States `loading/empty/error/tiptapConfigureError/success/validation DR-002/permissionDenied/conflict 409 + ReferenceList/draft/invalidBinding/404` + a11y keyboard `aria-label`/`aria-current`/`prefers-reduced-motion` + `video.webm` placeholder not break Nuxt | ERR-01..12, ALT-01..07, AC-005 |
| E2E-01 | E2E (video: 'on') — Master dedicated pages | `test/e2e/letter-builder-polish-master.spec.ts` (video.webm) | Create tabel Pegawai via `/dashboard/master-data/create` dedicated PageShell same layout → submit `POST /api/master-data` 201 → redirect list `Menampilkan` → Edit via `/dashboard/master-data/:slug/edit` same layout pre-filled → `PUT` → browse search/sort/visibility + create row → `GET /api/master-data/:slug/schema` → video artifact `test-results/**/video.webm` exists | Step 1-7, AC-001, BR-001/006/009, video `on` 1280×720 |
| E2E-02 | E2E (video: 'on') — Component Tiptap dedicated + configure guard + 409 | `test/e2e/letter-builder-polish-component.spec.ts` (video.webm) | Create Kop via `/dashboard/components/create` dedicated + Tiptap office-min toolbar each group `B/I/U/S`, Heading, Align, List, Table, Image `https://`, Link → binding `{{kop.nama}}` pill `bg-[#e8f2fd]` → Save `POST /api/doc-components` → Edit via `/dashboard/components/:id/edit` layout same pre-filled `is_looping` → `PUT` → Delete terproteksi via list `NPopconfirm` → `DELETE 409` `ReferenceList` modal `data-testid=conflict-references` `NTag 409` `Lihat` (Reuse 09) + `Tiptap` `configure undefined` mock guard no throw `Cannot read properties` → `NAlert Retry` fallback | Step 8-12, AC-002/006, FR-005/008, ERR-02/07, video |
| E2E-03 | E2E (video: 'on') — Template Builder polish | `test/e2e/letter-builder-polish-template.spec.ts` (video.webm) | Create Template via `/dashboard/templates/create` dedicated meta `name/code` → Builder polish `/dashboard/templates/:id` Library `NTree` searchable filter `Cari component...` → drag `Kop/Daftar` → Canvas warm `#f6f5f4` + `EmptyStateCard` → Properties live + `RepeaterEditor` `Pilih semua` indeterminate + Loop source=item → preview HTML drawer 600px + `POST /api/doc-templates/:id/preview-pdf` (preview PDF) → Publish `BR-002` + version bump `BR-005` → edit meta via `/dashboard/templates/:id/edit` same layout; responsive 1280/768/375 | Step 13-18, AC-003, REQ-G03, FR-003/006, video |
| E2E-04 | E2E (video: 'on') — Administration mapping DR-002 per-field | `test/e2e/letter-builder-polish-administration.spec.ts` (video.webm) | Create Administrasi via `/dashboard/administrations/create` dedicated per-field editor → pick `Template SK` → rows `letter.number`, `letter.tanggal` auto-generate → leave `letter.tanggal` empty → Save → assert `400` `Missing: letter.tanggal (DR-002)` → per-field `NFormItem` border merah `validationStatus="error"` + `feedback` + `NAlert type="warning" Mapping incomplete — missing: letter.tanggal` summary + `focus` first + `scrollIntoView` → fill `letter.tanggal→{kind:"system", ref:"current_date"}` → success `201` → Edit via `/dashboard/administrations/:id/edit` same layout per-field highlight → `PUT` | Step 19-22, AC-004, `DR-002` `ERR-06` `BR-004`, video |
| E2E-05 | E2E (video: 'on') — Wizard + polish + PDF | `test/e2e/letter-builder-polish-wizard.spec.ts` (video.webm) | Wizard `NSteps` vertical guided `Data → SK → Tanda Tangan → Review` (polish) → isi `nomor`, pilih pegawai `RelationPickerModal` multiple → `+ Tambah Step` N append → Review gabungan pagebreak concatenation → render HTML + PDF gabungan `POST /api/administrations/:id/runs` snapshot `template_version` → `FINAL` → `GET /api/documents/:id/pdf` `application/pdf` download + `document_number` 409 duplicate inline `NAlert warning` vs old `Server Error` | Step 23-24, AC-003/005, REQ-G05, BR-005/006, video |
| E2E-06 | E2E (video: 'on') — Errors & video & access & responsive & Tiptap guard | `test/e2e/letter-builder-polish-errors.spec.ts` (video.webm) | 401→`/login`, 403 single `data-testid=access-denied` (retain 1 hit via `grep addEventListener`), 404 slug `NEmpty+Kembali`, 500 PDF `Coba lagi` without reset `data/search/sort/page` (preview `preview-pdf` mock `pdfError`), Tiptap `configure` guard (`NAlert Gagal memuat editor` + `Retry` not `Cannot read properties`), mapping `DR-002` `missing: field` per-field, 409 `ReferenceList` `data-testid=conflict-references`, mobile 375 toolbar scroll-x + `+ Binding` 44px fallback, `prefers-reduced-motion` reduce, header `head 16×20 body 24` same layout dedicated create vs edit via `page.goto` direct + `video.webm` artifact assert `test-results/**/video.webm` exists `size 1280×720` per `https://playwright.dev/docs/videos#record-video` | ERR-01..12, ALT-05..07, EC-01..10, `BR-007/008/010`, AC-005/007/008, video `on` |

- [ ] Unit tests `UT-01..06` — semua DTO/service/helper + Tiptap guard `mod.default ?? mod` + `getMissingFields` 5-shape — 1 test per `FR/BR/DR/INV` (coverage ≥80% logic baru dedicated + DR-002 + Tiptap)
- [ ] Nuxt tests `NT-01..06` — semua states dedicated same layout + Tiptap office-min + builder polish + DR-002 per-field `missingFields` + DataTable kanonis + video no break + a11y keyboard/`aria-label`/`prefers-reduced-motion`
- [ ] E2E tests `E2E-01..06` polish — happy dedicated + Tiptap `configure` guard + builder polish + mapping DR-002 per-field + wizard + errors/permission/edge + mobile — semua `HEADLESS=1` Chromium `video: 'on'` `size 1280×720` `trace: 'on-first-retry'` + `reuseExistingServer: true` `:3000` + `HEADLESS=1` vs `CI=true` headed handling — **mapping 1:1 ke `User Flow` + `AC-001..008`** + `video.webm` exists per `test-results/`
- [ ] Coverage target: User Flow Steps 24/24 (100%), AC 8/8 (100%), BR 10/10 (100%), EC 10/10 (100%), ERR-01..12 (100%), Video 6/6 (100%)

