## Verification

> Verifikasi seolah QA independen. Pastikan semua User Flow dedicated pages + Tiptap `configure` guard + Builder polish + DR-002 per-field + video `on` berjalan benar. Dipakai `/verify` & `/review`.

### Automated (wajib lolos sebelum DONE)

- [ ] Typecheck (`vue-tsc --noEmit` / `nuxt typecheck` dari `apps/web/`) — 0 error — `npx vue-tsc --noEmit` (atau `nuxt prepare && vue-tsc`) via `better-sqlite3` ESM guard; `app/pages/dashboard/components/create.vue` + `.../[id]/edit.vue` + `templates/create.vue` + `templates/[id].vue` polish + `administrations/create.vue` + `administrations/[id]/edit.vue` + `ComponentEditor.vue` hardened `mod.default ?? mod` + `TiptapToolbar.vue` + `StepMappingEditor.vue` per-field `data.missingFields` + `playwright.config.ts` video `on` size 1280×720 — no `Cannot read properties of undefined (reading 'configure')` TS error.
- [ ] Unit tests (`npm run test:unit` dari `apps/web/`) — semua `UT-01..06` PASS, coverage ≥80% logic baru dedicated + DR-002 + Tiptap guard + image allowlist + DocNode limits — `test/unit` 6 suites `UT-01` doc-components Tiptap 8 + `UT-02` doc-templates publish 6 + `UT-03` master-data 6 + `UT-04` administrations `DR-002 data.missingFields` 8 + `UT-05` renderer sanitize + `UT-06` error helper `getMissingFields` + `tiptap` guard mock `undefined` → `NAlert Retry` (full `npm run test:unit` 257+6 polish PASS vs existing 257 PASS 09 regression no fail).
- [ ] Nuxt tests (`npm run test:nuxt` dari `apps/web/`) — semua `NT-01..06` PASS, semua states dedicated same layout + Tiptap office-min toolbar + Builder polish + DR-002 per-field `missingFields` highlight + DataTable kanonis + `AccessDeniedAlert` single + `prefers-reduced-motion` + video placeholder — full `npm run test:nuxt` 73+6 PASS (existing 73 PASS 09 + `NT-01` dedicated `PageShell` same `title B Batal|Simpan` + `NT-02` `data-testid=tiptap-toolbar` + `NT-03` `EmptyStateCard` + `NT-04` `StepMappingEditor` `NAlert warning missing: letter.tanggal` + `NT-05` `data-testid=access-denied` 1 hit + `NT-06` States polish 10) — `vue-tsc` 0 before.
- [ ] API/Integration tests (via unit service direct, not seeder): semua endpoint `POST/PUT/GET/DELETE` dedikasi + `400 DR-002 missing: field` `data.missingFields` array (exact) + `409` duplicate/references `data.references` + `400` Zod 13 tipe + `404` + `401/403` matrix (`requireApiAccess` `method+URL` `matchUrlPattern` allow/deny) PASS — `UT-04` DR-002 `POST /api/administrations` missing `letter.tanggal` → `400 data.missingFields=["letter.tanggal"]` exact + `E2E-04` live `missingFields` → per-field highlight `validationStatus="error"` + `NAlert` summary via `$fetch` error shape (`e.response.data.data.missingFields || e.data.missingFields`).
- [ ] E2E tests (`npm run test:e2e` HEADLESS=1 chromium `video: 'on'` size 1280×720 `trace: 'on-first-retry'` `reuseExistingServer: true` `:3000`) — semua `E2E-01..06` polish PASS (retries 2, `video: 'on'` per `https://playwright.dev/docs/videos#record-video`):
  - `letter-builder-polish-master.spec` (video.webm) — `create` via `/dashboard/master-data/create` dedicated PageShell same → 201 → redirect list → edit via `/:slug/edit` same layout pre-filled → browse search/sort/visibility
  - `letter-builder-polish-component.spec` (video.webm) — `create` via `/dashboard/components/create` dedicated + Tiptap office-min toolbar each group click `B/I/U/S Heading Align List Table/Image/Link History + Binding 44px` → pill `bg-[#e8f2fd]` → Save `POST` → Edit via `/:id/edit` same layout pre-filled → `PUT` version bump → delete 409 `ReferenceList` modal not `Server Error` + `Tiptap configure undefined` mock fallback `NAlert Retry` not crash
  - `letter-builder-polish-template.spec` (video.webm) — `create` via `/dashboard/templates/create` meta → Builder polish `/dashboard/templates/:id` Library `NTree` searchable filter `Cari component...` drag `Kop/Daftar` → Canvas warm `bg-[#f6f5f4]` `EmptyStateCard` → Properties live + `RepeaterEditor` pilih semua indeterminate + Loop source=item → preview HTML drawer 600px + preview PDF
  - `letter-builder-polish-administration.spec` (video.webm) — `create` via `/dashboard/administrations/create` per-field editor → pick Template SK → rows `letter.number` `letter.tanggal` auto → leave `letter.tanggal` empty → Save → assert `400 DR-002 missing: letter.tanggal` → per-field `validationStatus="error"` border merah `#EF4444` + `feedback` + `NAlert warning missing: letter.tanggal` + scroll & focus first → fill `letter.tanggal→{kind:"system", ref:"current_date"}` → success `201` → edit via `/:id/edit` same layout
  - `letter-builder-polish-wizard.spec` (video.webm) — Wizard `NSteps` vertical guided polish `Data → SK → Tanda Tangan → Review` → `+ Tambah Step` N → pagebreak → `POST /api/administrations/:id/runs` snapshot `template_version` → `GET /api/documents/:id/pdf` `application/pdf` + `document_number` 409 duplicate inline `NAlert warning` not `Server Error`
  - `letter-builder-polish-errors.spec` (video.webm) — 401→`/login`, 403 single `data-testid=access-denied` 1 hit + `grep addEventListener rbac-denied` 1, 404 slug `NEmpty+Kembali`, 500 PDF `Coba lagi` without reset `data/search/sort/page`, Tiptap `configure` guard `Gagal memuat editor`, mapping `DR-002` per-field, 409 `ReferenceList` `data-testid=conflict-references` `NTag 409`, mobile 375 toolbar scroll-x + `+ Binding` 44px fallback, `prefers-reduced-motion` reduce, header same layout dedicated vs edit via direct `page.goto` + `video.webm` artifact `size 1280×720`
  - + existing `letter-builder-*.spec.ts` 6 files (E2E-01 master, E2E-02 protect, E2E-03 component, E2E-04 template, E2E-05 wizard, E2E-06 errors) still PASS (no regression) — full suite `HEADLESS=1` 12+ specs PASS + `video.webm` per-test `test-results/**` size 1280×720 exists (via `ls -lh test-results/**/video.webm`) + `playwright-report/data` trace viewable `npx playwright show-report` + `show-trace`.
- [ ] **Playwright video artifact verification** — `grep -rn "video:" apps/web/playwright.config.ts` contains `video: { mode: 'on', size: { width: 1280, height: 720 } }` or `video: 'on'` + `trace: 'on-first-retry'` + `screenshot: 'only-on-failure'` per `https://playwright.dev/docs/videos#record-video`; `ls test-results/**/video.webm` ≥ `6` files (polish) + ≥12 (full suite) `size 1280×720`; `playwright-report` video `src` `attachment video`; `HEADLESS=1` vs `CI=true` headed handling preserved (`headless: !headed` + `slowMo headed 100 else 0`); `reuseExistingServer: true` `:3000` no `webServer` port conflict.
- [ ] Storybook build (`npm run build-storybook` dari `apps/web/`) — sukses tanpa error (chunks `LetterBuilderPolish` 7 files + `LetterBuilder` 9 files 08 + `foundation` 3 files di `storybook-static/index.json`, `LetterBuilderPolish/TiptapToolbar` + `AdministrationMapping` + `TemplateBuilderPolish` + `StatesPolish` ada, `vue-tsc` 0 before, `LetterBuilder/*` regression PASS + `LetterBuilderPolish/*` new PASS).
- [ ] Build (`npm run build` dari `apps/web/`) — sukses (chunk `~22MB` baseline ± `21.9MB` 09, no `Cannot read properties of undefined (reading 'configure')` runtime, `npx nuxt prepare` + `ls db.sqlite` 12+5 tables auto-sync `isMasterPhysicalTable` ignore `mst_*`).

### Manual / QA Checklist (mapping ke User Flow & AC)

- [ ] **Database verification** — entity 9+5 schemas (`orm-data-source.ts` `appEntities` + `appMigrations` `Baseline + CreateMasterTables + CreatePersuratanTables` + `isMasterPhysicalTable` ignore `mst_*` drift), constraint UNIQUE `name/slug/code/document_number` + `FK CASCADE` vs `RESTRICT` (`admin_steps.templateId` RESTRICT 409) — `DR-002` `mapping_json` per-field `data.missingFields` array via `UT-04` `POST /api/administrations` missing `letter.tanggal` → `400 data.missingFields=["letter.tanggal"]` exact.
- [ ] **Permission verification** — 401/403 matrix per `## API` (`/api/doc-components*` needs `Component Read/Write`, `/api/administrations*` `Admin Read/Write` `DR-002`, `/api/master-data*` `Master Data Read/Write`, etc.) — Viewer `Read` → `GET 200 POST 403` + `rbac-denied` `NAlert single` `data-testid=access-denied` 1 hit (`grep -rn addEventListener.*rbac-denied` 1), Admin `Write` → 200/400/409 per-field not `Server Error`; dedicated create page without permission → `AccessDeniedAlert` single + redirect not show form — `NT-01` + `E2E-06` video.
- [ ] **Business Rules verification** — `BR-001` slug regex → `UT-03` 400; `BR-002` publish guard `BR-002` → `UT-02` + `NT` `invalid binding` badge + E2E publish (E2E-03 video); `BR-003` loop `item.*` → `UT-01` 400 + `NT-02` badge; `BR-004` `DR-002` mapping completeness `missing: field` per-field `data.missingFields` → `UT-04` + `NT-04` `NAlert warning missing: letter.tanggal` + E2E-04 live `400` → `NFormItem` error `validationStatus` + scroll & focus first (not textarea); `BR-005` version bump → `UT-02` re-fetch version+1; `BR-006` `is_searchable/orderable` 400 + `document_number` unique 409 → `UT-03`+E2E-06; `BR-007` div-by-zero null+warning → `UT-03`+E2E-06; `BR-008` upload 5MB/1MB `DocNode` limit 400 → `UT-05`+NT; `BR-009` dedicated layout same `PageShell` head `16×20 body 24 radius 12 hairline` via `NT-01` `PageShell same`; `BR-010` Tiptap guard `if (!Ext?.configure) fallback` + `editor.destroy()` safe → `UT-06` mock `undefined` → no throw `Cannot read properties`.
- [ ] **Edge Cases verification** — `EC-01` unsaved guard `onBeforeRouteLeave` `NDialog` + draft autosave `localStorage:letter-builder:draft:<slug>` banner (`letter-builder-polish-dedicated`); `EC-02` extension import `undefined` fallback `StarterKit` minimal `NAlert Retry`; `EC-03` builder drag tablet no mouse keyboard `ArrowUp/Down` `aria-grabbed`; `EC-04` 50 requirements scrollable `NScrollbar` max-height 400 + filter; `EC-05` XSS `javascript:` `sanitize-html` allowlist; `EC-06` video disk penuh CI fallback `retain-on-failure` vs `on`; `EC-07` mobile 375 toolbar scroll-x `overflow-auto` + `+ Binding` bottom fixed 44px `data-testid=tiptap-toolbar`; `EC-08` `prefers-reduced-motion` `usePageTransition` 250ms → 0.01ms `app/assets/css/main.css`; `EC-09` 409 race delete after dedup → 404 not 500; `EC-10` `findReferences` exact `"componentId":"<name>"` guard `UT-01` not substring → all `EC-01..10` PASS via `NT-01..06` + `E2E-02/04/06` video.
- [ ] **States verification** — loading `NSpin`/`NSkeleton` + empty `NEmpty`+CTA pill primary `+ Buat ...` via `NDataTable #empty` single (Task02 single NEmpty) + error `NAlert Gagal memuat data + Coba lagi` retry without reset `search/sort/page` + `TiptapConfigureError` `NAlert error Gagal memuat editor (configure) + Retry` + `ClientOnly` fallback `Editor dimuat di sisi klien...` 60% + success `Berhasil` + validation inline `NFormItem` `validationStatus="error"` + `NAlert` summary per-field `DR-002` `missing: letter.tanggal` + focus & scroll first + permissionDenied single `data-testid=access-denied` + conflict 409 `ReferenceList` `data-testid=conflict-references` + `NTag 409` + `Lihat` + draft `NAlert info` + invalid binding red ring `border-[#EF4444]` + 404 `NAlert`+`NEmpty`+`Kembali` — `NT-01..06` + `StatesPolish` variants `LetterBuilderPolish` + `LetterBuilder` a11y PASS `prefers-reduced-motion` reduce.
- [ ] **Dedicated pages same layout manual replay** — `page.goto('/dashboard/components/create')` → PageShell `Buat Component` breadcrumb `Dashboard / Component / Buat` head `16×20` body `24` radius `12` hairline → fill `name=Kop Polिश` + Tiptap `Bold` → Save → `201` → list → `page.goto('/dashboard/components/1/edit')` → same `head 16×20 body 24 radius 12` + pre-filled same card `p-6` Level-1 `rgba(0,0,0,0.04) 0 4px 18px` + footer `Batal | Simpan` pill `9999px` — diff via screenshot `expect(page).toHaveScreenshot` pixel-perfect ± `maxDiffPixels 100` vs design 10 `mockups/component-create.png` + `component-edit.png` (only `title` diff); same for `templates/create` vs `templates/:id/edit` + `administrations/create` vs `:id/edit`.
- [ ] **Tiptap configure guard manual replay** — repro exact bug report `Cannot read properties of undefined (reading 'configure')` at `ComponentEditor.vue:40-69`: `page.goto('/dashboard/components/create')` → `editorError` not visible → no console error `Cannot read properties of undefined (reading 'configure')` (via `page.on('console', msg => expect(msg.text()).not.toContain('Cannot read properties'))`) + toolbar visible `data-testid=tiptap-toolbar` + `NButton` `B/I/U/S` etc.; mock `await import` return `undefined` via `page.route` → guard fallback → `NAlert error Gagal memuat editor (configure)` + `Retry` button → click `Retry` → re-init success without reload; delete via list `TrashCan` → `DELETE 409` → `ReferenceList` not `configure` crash.
- [ ] **Builder polish manual replay** — `page.goto('/dashboard/templates/2')` (id 2) → `grid-cols-[260px_1fr_320px]` desktop 1280 (via `evaluate getComputedStyle gridTemplateColumns`) → Library `NTree` filter `NInput` 32px `Cari component...` → type `Kop` → filter works; Canvas `bg-[#f6f5f4]` soft + `EmptyStateCard` illustration warm `xl16` `p-32` when empty → drag `Kop` from Library `NTree` → Canvas ghost `opacity 0.5` ring `bg-[#e8f2fd]` `border-dashed #0075de` → drop → block appears; Properties `NForm` eyebrow `11px uppercase #94a3b8` + `RepeaterEditor` header `Pilih semua` indeterminate checked vs unchecked; keyboard `ArrowUp/Down` reorder `aria-grabbed="true"`; preview drawer `NTabs HTML|PDF` `width 600` + `NScrollbar` + `NCode`.
- [ ] **DR-002 per-field manual replay** — `page.goto('/dashboard/administrations/create')` → `NInput name=SK Polिश` `slug=sk-polish` → add Step 1 `NSelect template=SK Pengangkatan` → per-field rows `letter.number` + `letter.tanggal` auto via `GET /api/doc-templates/:id` `scanRequirements` → leave `letter.tanggal` empty `kind` default `value` `ref` empty → `Simpan` → `400 Missing: letter.tanggal (DR-002)` → assert `NFormItem` `letter.tanggal` `validationStatus="error"` border `#EF4444` + `feedback` + `NAlert type="warning" Mapping incomplete — missing: letter.tanggal` + `ref` focused + scroll position `element.getBoundingClientRect().top` near viewport center; fill `letter.tanggal → kind:system ref:current_date` → resubmit → `201` + toast `Berhasil` + redirect list + menu `[SK Polिश]`; edit via `/dashboard/administrations/:id/edit` same layout + same per-field editor + same `DR-002` highlight when clear → video recorded `letter-builder-polish-administration.spec` `video.webm`.
- [ ] **Responsive verification** — dedicated pages `grid-cols-2` desktop 1280 vs `grid-cols-1` tablet 768 vs `grid-cols-1` mobile 375 + Builder 3-pane `260|1fr|320` vs drawer 260/320 vs `NTabs Library/Canvas/Properties` + toolbar group `flex-wrap` desktop vs `overflow-auto` scroll-x mobile 375 vs `+ Binding` bottom fixed 44px — wireframe `desktop/tablet/mobile.svg` + Storybook viewport addon 1280/768/375 screenshot `expect(page).toHaveScreenshot` + Playwright `test.use({ viewport })` 3 projects vs `video` 1280×720 not break mobile — AC-D03 `10` not regression.
- [ ] **Accessibility verification** — keyboard Tab `+ Buat Component` dedicated → form `name` → `is_looping` `Checkbox` → Tiptap toolbar `B` `Tab` → `I` → `U` → `H2` `NSelect` → `Align Center` → `Table` → `+ Binding` 44px → `Simpan` → `Esc` close modal/drawer/toolbar bubble + focus trap `NModal`/`NDrawer` + `NSteps` `aria-current="step"` + `aria-grabbed` draggable + `aria-label` icon-only (`Segarkan data` `Bold` `Tambah step` `Tutup`) + live region `role="status"` toast `Berhasil` + contrast Ink/Canvas 18:1 + primary 4.6:1 AA + `prefers-reduced-motion` 0.01ms via `app/assets/css/main.css` `animation-duration 0.01ms` — Storybook a11y addon 0 critical + `axe-core` `toHaveNoViolations`.
- [ ] **UI/UX verification** — pixel-perfect vs `10` mockup + **Storybook** `apps/web/stories/letter-builder-polish/*.stories.ts` 7 files (Naive UI direct import `import { NButton, NForm, NInput, NSelect, NTree } from 'naive-ui'` + Tailwind `bg-[#f6f5f4] border-[#e6e6e6] text-[#0075de]` + `NConfigProvider` `themeOverrides` Notion `#0075de` + `withProviders.ts` `pinia` + `$fetch` mock) + `foundation/DataTable` toolbar `320/160` + PageShell `head 16×20 body 24 radius 12` + sidebar `220/72` token `#0075de` active `#e8f2fd` border + `EmptyStateCard` warm `#f6f5f4` xl16 + pill CTA primary — no hard-coded color out of token, no `NDescriptions` (`.detail-view`), icon `h(NIcon)` Carbon consistent, `TiptapToolbar` 1 bar grouped very minimum office.
- [ ] **Storybook verification** — `npm run storybook` `:6006` menampilkan `LetterBuilderPolish/*` 7 stories (`MasterDataCreatePage`, `ComponentCreatePage`, `ComponentEditPage`, `TiptapToolbar`, `TemplateBuilderPolish`, `AdministrationMapping`, `StatesPolish`) × variants default/loading/empty/error/validation/permissionDenied/conflict/tiptapConfigureError/draft + `LetterBuilder/*` 9 stories 08 + `foundation/*` 3 files + `redesign/*` 8 files — controls + viewport 1280/768/375 + a11y 0 critical; `npm run build-storybook` sukses zero error chunks `LetterBuilderPolish` + `LetterBuilder` + `foundation` in `storybook-static` `index.json`.
- [ ] **Video verification** — `playwright.config.ts` `use: { video: { mode: 'on', size: { width:1280, height:720 } }, trace: 'on-first-retry', screenshot: 'only-on-failure' }` + `headed` `slowMo` + `webServer` `reuseExistingServer: true` via `grep -rn "video:" apps/web/playwright.config.ts`; `HEADLESS=1 npm run test:e2e -- letter-builder-polish` → `video.webm` per spec `test-results/**/video.webm` `size 1280×720` (via `ffprobe` or `file` size) + `playwright-report/data` + `trace.zip` on retry; `CI=true` fallback documented `retain-on-failure` disk tradeoff; existing `letter-builder-*.spec.ts` 6 files still PASS with video `on` not `retain-on-failure` only.
- [ ] **User Flow verification** — setiap `User Flow > Steps 24` + `Alternate ALT-01..07` + `Error ERR-01..12` + `Flow→UI/API Mapping` dedicated + Tiptap guard + Builder polish + DR-002 per-field → ada `AC-001..008` + ada `E2E-01..06` polish `video: 'on'` PASS + `UT-01..06` + `NT-01..06` + traceability `User Flow ↔ AC ↔ Test ID` 100% tabel `Test Plan` `AC-001↔E2E-01↔UT-03/NT-01` etc.
- [ ] **Acceptance verification** — setiap `AC-001..008` Given/When/Then PASS + `grep -rn "video.*mode.*on" playwright.config.ts` 1 hit + `grep -rn "mod.default ?? mod" app/components/features/persuratan/ComponentEditor.vue` 1 hit + `grep -rn "StepMappingEditor" app/pages/dashboard/administrations` 2 hits `create.vue`+`edit.vue` + `grep -rn "TiptapToolbar" ComponentEditor.vue` 1 hit + `grep -rn "MasterDataCreatePage\|ComponentCreatePage" stories/letter-builder-polish` 2 hits + `grep addEventListener rbac-denied` 1 hit + `grep data-testid=conflict-references` 4 hits + `grep data-testid=access-denied` 1 hit + `grep missingFields` server `administrations.service.ts` + `getMissingFields` `app/utils/error.ts` — `tasks/task-logs.md` detail updated.

## Assumptions

- FASE 1 `10` sudah TODO (wireframes 18 SVG + stories 7 files `LetterBuilderPolish/*` + `TiptapToolbar` + `StepMappingEditor` mock — belum `DONE` + belum `build-storybook` PASS; jika `10` belum `DONE` maka blocking `11` `TODO` — design stub reuse `10` wireframe SVG + Storybook variant `tiptapConfigureError` etc. sebagai acuan; FASE 2 wiring tidak redesign token).
- `Puppeteer Chrome` mungkin belum ter-install di CI/dev awal → `POST .../preview-pdf` E2E step PDF di-skip via `test.skip` bila `!chrome` (mock `pdfError` + `NAlert Retry` contract `200` with `pdfError` field tetap PASS), tetapi **kriteria `AC-003` preview HTML tetap live**; video artifact tetap record `PDF retry` UI even if Chrome skip (video shows `NAlert Retry`).
- `@vueuse/core`/`vue-draggable-plus` diasumsikan **optional helpers** — bila belum `npm install` maka fallback HTML5 `draggable` native (`DocumentCanvas` `draggable="true"` + `data-transfer`) tetap lolos design (`10` Library rationale both listed); FASE 2 install only if `useStorage/useDraggable` smooth without token change.
- `error.response.data.data.missingFields` vs `error.data.missingFields` vs `error.response.data.missingFields` — backend `createError` `data:{ missingFields }` → `$fetch` `h3` wrap shape varies (`error.response.data.data` or `error.data`); helper `getMissingFields(e)` handle 5 shapes same as `getConflictReferences` (already 5-shape in 09) — fallback generic `NAlert warning` if array empty still `warning`.
- `localStorage` keys: `localStorage:master-data:<slug>:visibility` (min1 guard `disable last hide` + tooltip `"Minimal satu kolom"`), `localStorage:letter-builder:draft:<slug>` wizard draft banner `DRAFT` `NAlert info` + `localStorage:letter-builder:draft:component|template|administration` autosave dedicated create `watchDebounced` not `auth` `user`/`accessToken` existing (`auth.ts` store) no conflict via `try/catch JSON.parse`; `NModal` `v-model:show` `NAlert warning` `data.missingFields`.
- `playwright.config.ts` `video: { mode: 'on', size: { width:1280, height:720 } }` assumed `1280×720` same as `viewport` `1280×720` per `https://playwright.dev/docs/videos#record-video` example `video: 'on' size 1280×720` → artifact `video.webm` `1280×720` VP8; if CI disk limit high, doc notes fallback `mode: 'retain-on-failure'` + `trace: 'on-first-retry'` for 6× `video.webm` ~ 6MB each (vs `retain-on-failure` 0 on pass) — decision env `HEADLESS=1` `CI=true` existing `playwright.config.ts:5 headed`.
- `Tiptap` SSR → `ClientOnly` wrapper wajib — `ComponentEditor.vue` not imported in `test:unit` node env (only `test:nuxt` client), prevent `document is not defined` + `Editor` `element: editorHost.value` only after `onMounted` guard `if (!editorHost.value) return` + `editorReady` flag.
- `vue-tsc` strict `true` (`nuxt.config.ts:typescript.strict`) — `Record<string,unknown>` + `statusCode?: number` typed `Error & { statusCode?: number; data?: unknown; response?: { data?: unknown } }` union existing `app/utils/error.ts` `isConflictError/getConflictReferences/getMissingFields` typed `unknown` → `as` safe, not `any` global.
- Wire `TiptapToolbar.vue` extracted from `ComponentEditor.vue:146 toolbar` old `TOOLBAR 11` list `B/I/U/H2/• List` → new 1 row grouped 8 groups via `NSpace` + `NButton` `size="small"` + `h(NIcon)` `Carbon` `TextAlignCenter` etc. — `runAction` switch `bold/italic/underline/strike/heading/bulletList/orderedList/table/image/align/undo/redo/link` plus `setHighlight` `toggleHighlight` `setColor` very minimum (2 color `TextColor` `Highlight` vs full palette heavy Typography `CharacterCount`).
- Dedicated pages file routing Nuxt 4 `app/pages/dashboard/components/create.vue` vs `app/pages/dashboard/components/[id]/edit.vue` — ensure `definePageMeta({ layout:'default', middleware:'auth', requiresAuth:true })` + `useRoute().params.id` `Number` guard vs `NaN` → 404 `NEmpty+Kembali`; `create` vs `edit` same `PageShell` props verified `NT-01` screenshot diff ≤ 5% (only title).

## Open Questions

- Apakah `409` sebaiknya divisualkan sebagai **`NAlert type="warning"` (kuning `#FFFBEB/#FDE68A` `#D97706`)** atau `type="error"` (merah) untuk delete? `10/conflict.svg` warn warning — `11` wiring memakai `warning` agar beda dari 500 `error`; jika review pilih `error`, cukup ganti prop `type` tanpa Test ID ubah `data-testid=conflict-references` tetap.
- Perlu `inline link "Lihat → /dashboard/templates/:id"` vs `search` filter untuk `ReferenceList` `component:*` (`/dashboard/components?search=<name>`)? Deep-link ke `edit` page `/dashboard/components/:id/edit?highlight=form.name` diperlukan vs `search` only?
- `findReferences` exact `"componentId":"<name>"` cukup atau perlu `JSON.parse(schemaJson)` traversal tree untuk 100% akurasi (EC-10)? Traversal `O(n*m)` vs `includes O(n)` — benchmark 200 templates × 200 nodes vs 1MB `tiptap_json` — FASE 2 keep `includes` exact per 09 (fast) vs `parse` exact for future.
- `document_number` duplicate 409 apakah lebih baik inline `NFormItem` merah di wizard `AdminWizard.vue` field `document_number` (`validationStatus="error"`) daripada `NModal ReferenceList`? Contract runs `409 data.document_number exists` → wizard field focus vs modal — `11` keeps inline warning (reused `getMissingFields` not).
- Butuh `DELETE /api/master-data/:slug/columns/:name` granular atau tetap via `PUT syncColumns` diff? `11` polish `MasterTableForm` column delete via `PUT` remove `syncColumns` 409 `data.references` reuse 09; granular endpoint belum ada — perlu spec `11` follow-up?
- Apakah `ReferenceList` modal perlu `NScrollbar max-height 320px` + `virtual scroll` + `search filter` jika `references` >50 (Master Table dipakai 50 template)? `11` `NScrollbar` 320px no virtual yet; `NList` vertical fine.
- `NPopconfirm` delete → 409 modal flow: apakah `NPopconfirm` tetap dipakai sebagai guard awal atau diganti langsung `NDialog confirm` yang bisa switch ke `ReferenceList` bila 409 tanpa double modal jarring? `11` keeps `NPopconfirm` then `NModal` (double) — UX double-modal documented `10/Open Questions` vs `NDialog` single? Decision follow 09 pattern `NPopconfirm` + `NModal`.
- `Puppeteer` install CI: tetap `PUPPETEER_SKIP_DOWNLOAD=0` + `npx playwright install chromium` vs `npm install puppeteer --force` + `Xvfb`? `playwright.config.ts:headless` vs `CI=true` existing — `STORAGE_DIR` write test via `apps/web/storage` reuse no new folder.
- **Video**: `video: 'on'` artifact per-test `video.webm` 6× ~1MB each → total 6MB per run + `trace.zip` on retry 2× → disk `20MB`; bila CI artifact limit `500MB` apakah perlu `video: 'retain-on-failure'` untuk `CI=true` env branch vs `HEADLESS=1` local `on`? Design 10 `video: 'on'` default but verification notes fallback `retain-on-failure` documented — FASE 2 wiring choose `on` for local + `retain-on-failure` for CI branch via `process.env.CI === 'true' ? 'retain-on-failure' : 'on'` — perlu update `playwright.config.ts` logic?
- **Dedicated page autosave**: `watchDebounced` `localStorage:letter-builder:draft:component` every `500ms` vs `onBeforeRouteLeave` only? Autosave `Draft tersimpan otomatis — Lanjutkan?` banner reuse `AdminWizard` pattern vs dedicated create no banner — perlu unify?
- **Toolbar very minimum**: `Highlight`/`Color` `setHighlight({color:'#ff0'})` vs `TextStyle` `setColor('#0075de')` — apakah perlu 2 atau cukup `Bold/Italic/Underline/Strike/Heading/List/Table/Align/Image/Link` sebagai `very minimum office` tanpa `Highlight/Color`? `11` includes `Highlight` minimal (1 color `#ff0`) but can toggle via Storybook `args`.

## Related Knowledge

- `tasks/10-letter-builder-polish-ui-design/README.md` + `spec.md` + `flow-requirements.md` + `domain-api-ui.md` + `acceptance-tasks.md` + `verification.md` — FASE 1 UI-First polish (wireframes 18 SVG + stories 7 files `LetterBuilderPolish/*` + `TiptapToolbar` + `StepMappingEditor` + Playwright video `E2E-01..06` + `https://tiptap.dev/docs/examples` + `https://playwright.dev/docs/videos#record-video`).
- `tasks/08-letter-builder-ux-improvement-ui-design/*` + `tasks/09-letter-builder-ux-improvement/*` — baseline Letter Builder hardening (wireframes 8 SVG + stories 9 files + 409 ReferenceList + E2E 6 files) — polish merevisi 08 layouts ke dedicated pages + Tiptap guard + mapping per-field (polish 10 → 11 wiring).
- `tasks/05-document-engine/{spec,flow-requirements,domain-api-ui,acceptance-tasks,verification}.md` (engine JSON Tree 18 type + `ExpressionService` `++ "" * / + -` + renderer `sanitize-html` + PDF `Puppeteer` + DocNode limit + `ERR-04` 500 skip Chrome) + `tasks/06-master-data-ddl/*` (meta `master_tables/columns` fisik `mst_*` `master-ddl.service` sanitize/blacklist 13 tipe discriminated relation picker `master-operation.ts` eval) + `tasks/07-template-administration/*` (doc_components/templates Tiptap `ClientOnly` + `tiptap-converter` binding + Builder 3-pane 260|1fr|320 + `NTree` + `AdminWizard` `NSteps` + `executeRun` pagebreak + seeder demo).
- `docs/PRD.md` §4-10 (RBAC) + §17.6 Table Browse + §18 Alur Authorization + §19-20 API/Routes + §22 Seed — RBAC-Only goals auditability.
- `docs/architecture.md` § RBAC Architecture (9 schemas/12 tables) + § Nuxt 4 Project Structure + § API Endpoints (Auth/Users/.../Settings/Storage) + § RBAC System (`requireApiAccess` `matchUrlPattern` `permissionMatrix`) + § Table Browse Component (`DataTable.vue:320/160+Restart+error slot` + `useDataTable`) + § PageShell (`PageShell.vue` `head16×20 body24 radius12`) + § Storybook Foundation + AppLayout sidebar 220/72 + § E2E Testing (`HEADLESS` `SLOWMO_MS` `video retain-on-failure` → `on` polish) + `playwright.config.ts:1-32` trace.
- `docs/database.md` § ERD + § Entity Details 1-12 + § Seed Data + § Relationships (9 EntitySchemas RBAC + `master_tables/columns` + `doc_*` + `mst_*` + junctions + migrations `Baseline1700000000001 + CreateMasterTables + CreatePersuratanTables` + `isMasterPhysicalTable` ignore `mst_*` drift) + `DR-002` mapping completeness `mapping_json`.
- `docs/design-system.md` § Color Palette (Notion `#0075de/#0069c4/#005bab` + canvas `#f6f5f4/#ffffff` + hairline `#e6e6e6` + Ink Sticker + Typography Inter + Scaling xxs4-xxl32 + Radius xs4/full `12` + Elevation Level1/2 + Table + System Logs + PageShell + Sidebar + Animation `prefers-reduced-motion` + Chrome Patterns (Badge Pill, Empty-State Card, Toast, Auth Card, Modal Card, App-Shell Row) + Foundation Deliverables.
- `AGENTS.md` — EntitySchema + plain-object service + Zod DTO + Nitro `defineEventHandler` + `QuerySchema` + `createError` (`statusCode ?? 500` + `data`) + `getErrorMessage` + `isConflictError/getConflictReferences/getMissingFields` typed `unknown` + Tailwind v4 no-preflight + Naive UI direct import `import { NButton } from 'naive-ui'` + `h(NIcon)` + `better-sqlite3` + JWT 24h + `useApi` + `useAuthorization`.
- Kode kanonis existing: `apps/web/app/pages/dashboard/components.vue:1-198` (`NModal` create `showForm` + `ComponentEditor` import + `store.createComponent/updateComponent` + `ReferenceList` 409 09), `apps/web/app/pages/dashboard/templates/index.vue:1-184` (`NModal` create `showCreate` + `ReferenceList`), `apps/web/app/pages/dashboard/administrations.vue:1-195` (`NDynamicInput mappingText textarea JSON` `DR-002` 400 generic → polish `StepMappingEditor.vue` per-field), `apps/web/app/pages/dashboard/templates/[id].vue:1-121` (builder grid minimal `gap-4 lg:grid-cols-[260px_1fr_320px]` → polish warm + `EmptyStateCard` + `NTree` searchable), `apps/web/app/components/features/persuratan/ComponentEditor.vue:1-167` (Tiptap `StarterKit`/`Link.configure`/`TextAlign.configure`/`Table.configure` `DocBinding`/`DocRepeater`/`DocCondition` `ClientOnly` + `handleDOMEvents contextmenu` + `onUpdate emit update:modelValue` → hardened guard `mod.default ?? mod` + `try/catch` + `onBeforeUnmount destroy`), `apps/web/app/utils/tiptap-nodes.ts:1-67`, `apps/web/app/components/features/persuratan/{TemplateBuilder,ComponentLibrary,DocumentCanvas,PropertyPanel,RepeaterEditor,ConditionEditor,DocumentPreviewDrawer,AdminWizard}.vue`, `apps/web/app/utils/error.ts` (`getErrorMessage` + `isConflictError/getConflictReferences/getConflictData` + new `getMissingFields`), `apps/web/server/services/{doc-components,doc-templates,master-data,master-ddl,administrations,expression,renderer,pdf,tiptap-converter}.service.ts` + `server/dto/{master-data,persuratan,documents}.dto.ts` (`DR-002` `CreateAdministrationSchema`) + `server/api/{master-data,doc-components,doc-templates,administrations,documents}/**` + `server/entities/{master-table,master-table-column,doc-component,doc-template,administration,admin-step,document}.entity.ts` + `server/utils/{db,orm-data-source,route-guard,url-matcher,permission-matrix,migration-status}.ts` + `apps/web/app/stores/{master-data,persuratan}` + `apps/web/playwright.config.ts:1-32` (`headed` `slowMo` `video: 'retain-on-failure'` → `video: { mode: 'on', size: { width:1280, height:720 } }` per `https://playwright.dev/docs/videos#record-video`) + `shared/types/{master-data,persuratan,document}.ts` + `apps/web/stories/letter-builder/*` 08 + `apps/web/stories/letter-builder-polish/*` 10 (design).
- `app/utils/naiveui-theme.ts` + `app/assets/css/main.css` (`tailwindcss/theme` + `utilities`, no preflight, `:root` token, `prefers-reduced-motion`) + `apps/web/.storybook/{main.ts,preview.ts}` (stories `../stories/**/*.stories.*`, addons `a11y`+`docs`, `vue3-vite`, decorator `NConfigProvider` + `themeOverrides` Notion).
- `https://tiptap.dev/docs/examples` — referensi toolbar office-min (StarterKit Typography Placeholder TextAlign Highlight Color Link Image Table BubbleMenu FloatingMenu Typography CharacterCount Collaboration very minimum).
- `https://playwright.dev/docs/videos#record-video` — referensi video recording (`video: 'on' | 'retain-on-failure' | 'off'`, `videoSize` `trace` `screenshot` `contextOptions` `testInfo.attach` `playwright-report` `headed` `reuseExistingServer` `slowMo` `viewport`).
- `.ua/` knowledge graph 754 nodes.

## Change Log

### Initial — FASE 2 Implementation (Routing Dedicated Pages + Tiptap Hardening + Builder & Mapping Polish + Video)

- Task specification created (FASE 2 — Implementation: wire `10-letter-builder-polish-ui-design` dedicated pages same layout + Tiptap `configure` guard fix + Builder `/dashboard/templates/:id` 3-pane polish + DR-002 per-field mapping editor + global UI/UX polish + Tiptap office-min toolbar + Playwright `video: 'on'` 1280×720). Sumber: user prompt 2026-09-14 — `buatkan juga task untuk FASE 2 FASE 2 Implementation: routing dedicated pages + Tiptap hardening + builder/mapping polish + playwright.config.ts video: 'on' + E2E-01..06 video artifacts` — ditafsir sebagai polish Letter Builder lanjutan `10` dengan UI-First wiring (routing 6 dedicated pages + `ComponentEditor` hardened `mod.default ?? mod` + `TiptapToolbar` grouped very minimum + `StepMappingEditor` per-field `data.missingFields` + Builder warm `EmptyStateCard` + video `https://playwright.dev/docs/videos#record-video` `video: { mode: 'on', size: 1280×720 }` + E2E video 6). Deliverables: 6 dedicated PageShell pages (`components/create`+`[id]/edit`, `templates/create`+`[id]/edit`, `administrations/create`+`[id]/edit` + `master-data/create` refine) + `app/components/features/persuratan/{ComponentEditor.vue hardened + TiptapToolbar.vue + StepMappingEditor.vue}` + `app/pages/dashboard/templates/[id].vue` polish + `app/utils/error.ts` `getMissingFields` + `playwright.config.ts` `video: 'on'` + tests `UT-01..06 NT-01..06 E2E-01..06 polish video` `HEADLESS=1` `reuseExistingServer: true` + Storybook `LetterBuilderPolish/*` regression `10` + `LetterBuilder/*` 08 + `foundation/*` . Next: `/plan tasks/11-letter-builder-polish/README.md` → `/implement` (wiring `ComponentEditor` guard no `Cannot read properties` + `TiptapToolbar` office-min very minimum + `StepMappingEditor` per-field `missing: letter.tanggal` + Builder `260|1fr|320` warm + video `video.webm`) → `/verify` (`vue-tsc` 0 + `test:unit` + `test:nuxt` + `test:e2e` HEADLESS `video.webm` `size 1280×720` + `build` + `build-storybook`) → `/review` — sebelum DONE pastikan `AC-002` Tiptap `configure` guard `try/catch` + `AC-004` DR-002 per-field `missingFields` + `AC-007` `video: { mode: 'on', size }` PASS + `video.webm` per-test exists.

