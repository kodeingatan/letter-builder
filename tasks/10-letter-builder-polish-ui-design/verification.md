## Verification (Design)

- [ ] **Design System verification** (token `app/utils/naiveui-theme.ts` Notion `#0075de`/`#0069c4`/`#005bab`/`#f6f5f4`/`#ffffff`/`#e6e6e6`/Inter/radius xs4/full `12`, Naive UI direct import `import { NButton, NDataTable, NForm, NModal } from 'naive-ui'` + Tailwind utilities inline `bg-[#f6f5f4] border-[#e6e6e6] text-[#0075de]`, no `NDescriptions` — pakai `.detail-view` + `NForm`, icon `h(NIcon)` Carbon, `usePageTransition` Anime.js 250ms) — cek visual vs `docs/design-system.md` Chrome Patterns + Do's/Don'ts + `naiveui-theme.ts` live.
- [ ] **Dedicated pages verification** — setiap `create` vs `edit` pair (`master-data/create` vs `master-data/:slug/edit`, `components/create` vs `components/:id/edit`, `templates/create` vs `templates/:id/edit`, `administrations/create` vs `:id/edit`) rendering `PageShell` identik (title `20px Semibold tracking -0.125px Ink` + breadcrumb `<a href>` + description `12px #615d59` + head `16×20` body `24` radius `12` hairline) — cek `NForm` gap `12` + footer sticky `Batal | Simpan` pill `9999px` primary `#0075de` vs secondary white — no modal sebagai jalur utama (modal hanya delete `ReferenceList` + picker).
- [ ] **Tiptap configure guard verification** — `ComponentEditor.vue` `initEditor` guard `configure` undefined: simulasikan `await import('@tiptap/starter-kit')` → `undefined` → no throw `Cannot read properties of undefined (reading 'configure')` → `NAlert error "Gagal memuat editor"` + `NSpin` + `#fallback` + `Retry` re-init; `onBeforeUnmount` destroy guard; `ClientOnly` SSR safe; `TiptapToolbar.vue` grouped `B/I/U/S | H1-H3 | Align | List | Table/Image/Link | ↶↷ | + Binding` matches `https://tiptap.dev/docs/examples` minimal (StarterKit/Placeholder/Highlight/Color/TextAlign/Underline/Link/Image/Table + BubbleMenu + FloatingMenu) — Storybook `TiptapToolbar` + `tiptapConfigureError` variant pass.
- [ ] **Builder polish verification** — `/dashboard/templates/:id` 3-pane `260|1fr|320` desktop grid polish (Library `NTree` searchable filter `NInput` 32px `Cari component...` + Canvas `bg-[#f6f5f4]` soft `p-4` + `EmptyStateCard` warm `xl16` `p-32` `NEmpty` + CTA `+ Tambah Blok` pill + illustration + Properties `NForm` live sectioned eyebrow `11px uppercase #94a3b8` + `RepeaterEditor` pilih semua indeterminate + drag ghost `opacity 0.5` ring `#0075de` + keyboard `ArrowUp/Down` reorder `aria-grabbed` + preview drawer 600px tabs `HTML/PDF` `NCode` + `NScrollbar` + `NAlert` warning) — wireframe `builder-3pane.svg` + `desktop/tablet/mobile.svg` + Storybook `TemplateBuilderPolish`.
- [ ] **Administrasi mapping DR-002 verification** — `/dashboard/administrations/create` per-field editor: per-step `NSelect` template + rows `field NTag` badge + `kind NSelect (value/master_data/system)` + `ref NInput/NSelect` + inline `validationStatus="error"` + `feedback "Missing: letter.tanggal (DR-002)"` + `NAlert type="warning" Mapping incomplete — missing: letter.tanggal` summary list + focus first error + scrollIntoView smooth + tidak ada textarea JSON mentah — Storybook `AdministrationMapping` variant `validation` + wireframes `administration-create.svg`/`validation.svg`.
- [ ] **Responsive verification** — desktop ≥1024 (dedicated 2-col, builder 260|1fr|320, toolbar flex-row, picker 800px, mapping 2-col), tablet 768–1023 (drawer 260/320 + `NSteps` horizontal + mapping 1-col + modal 90vw), mobile <768 (grid 1-col, `NTabs Library/Canvas/Properties`, toolbar column + search full-width min 320px wrap, modal `100vw` full, Tiptap toolbar scroll-x `overflow-auto` + `+ Binding` bottom fixed 44px) — wireframes `desktop.svg` + `tablet.svg` + `mobile.svg` + Storybook viewport addon 1280/768/375 screenshot.
- [ ] **Accessibility verification** — keyboard Tab → `+ Buat` → form field → `Simpan` → `Coba lagi` → `ReferenceList Tutup` + Esc close modal/drawer/toolbar bubble, focus trap `NModal`/`NDrawer`, `Tab` order logis, `NSteps` `aria-current="step"` + `aria-grabbed` draggable + `aria-invalid`+`aria-describedby` per-field DR-002 + `aria-label` icon-only (`Segarkan data`, `Bold`, `Tambah step`, `Tutup`) + live region `role="status"` toast + contrast Ink `#000000` on `#f6f5f4` 18:1 + primary `#0075de` on white 4.6:1 AA + `prefers-reduced-motion` 0.01ms `app/assets/css/main.css` `@media (prefers-reduced-motion: reduce)` `animation-duration 0.01ms` — Storybook addon a11y 0 critical + manual keyboard walk 24 steps tanpa mouse.
- [ ] **User Flow coverage** — semua Step 1–24 + ALT-01..07 + ERR-01..12 + BR-001..010 + EC-01..10 ada di Storybook prototype — klik tanpa dead-end, play functions `userEvent` reach tiap state (dedicated create → edit layout same → builder drag → mapping missing → publish → wizard) + breadcrumb back works + `video` placeholder artifact.
- [ ] **Storybook verification** — `npm run storybook` (:6006) tampil kelompok `LetterBuilderPolish/MasterDataCreatePage`, `ComponentCreatePage`, `ComponentEditPage`, `TiptapToolbar`, `TemplateBuilderPolish`, `AdministrationMapping`, `StatesPolish` × variants (`default/loading/empty/error/validation/permissionDenied/conflict/tiptapConfigureError/draft/invalidBinding`) + `LetterBuilder/*` 08 regression + `foundation/*` 3 files; `npm run build-storybook` sukses (Vite 10.28s, chunks `LetterBuilderPolish` + `LetterBuilder` di `storybook-static/index.json`, tanpa error, `storybook-static` contains `video` docs).
- [ ] **Playwright video verification** — `playwright.config.ts` contains `video: { mode: 'on' }` atau `video: 'retain-on-failure'` + `trace: 'on-first-retry'` + `size: { width: 1280, height: 720 }` + `use.baseURL http://localhost:3000` + `headless = HEADLESS !== '1' && CI !== 'true'` + `slowMo headed 100` + `webServer npx nuxi dev --port 3000 reuseExistingServer true` — doc reference `https://playwright.dev/docs/videos#record-video` section `Record video` + `video` artifact `test-results/**/video.webm` + `playwright-report` trace viewer — E2E `E2E-01..06` design table maps `User Flow` ↔ `AC` ↔ `Video`.

### Manual / QA Checklist (mapping ke User Flow & AC)

- [ ] **Token & library audit** — Tiptap office-min toolbar (B/I/U/S, H1-3 select, Align Left/Center/Right/Justify, List •/1., Table, Image, Link, Color/Highlight, Undo/Redo, Placeholder `Tulis konten surat...`, CharacterCount) + `BubbleMenu` + `FloatingMenu` + `NTree` searchable + `NSteps` vertical guided + `NDynamicInput` kolom + `NUpload` dragger + `NDatePicker` `m-d-Y` + `StepMappingEditor` per-field + `TiptapToolbar.vue` grouped — semua direct import `import { NButton, NIcon } from 'naive-ui'` + `h(NIcon)` Carbon, no extra UI lib selain `@vueuse/core` helper (design rationale `https://tiptap.dev/docs/examples` tercatat) — AC-D06.
- [ ] **Wireframe completeness** — low-fi semua halaman polish × 3 breakpoint + dedicated pages (10 pages) + 10 state di `wireframes/` (18 SVG) — AC-D01/02/03/04.
- [ ] **Mockup token compliance** — hi-fi `mockups/` + Vue stub adherence pill CTA `#0075de` full `9999px`, input `#ffffff` xs4 `4px`, card lg12 `12px` + hairline `1px #e6e6e6` + Level-1 shadow `rgba(0,0,0,0.04) 0 4px 18px`, canvas `#f6f5f4` soft, app-shell row active `#e8f2fd` border `#0075de` — AC-D05.
- [ ] **Dedicated pages navigability** — klik happy path polish (Master `create` → list → `edit` same layout → Component `create` dedicated Tiptap → `edit` same layout → Template `create` meta → Builder polish → Administrasi `create` per-field mapping → `edit` same layout → Wizard +Tambah Step N → PDF gabungan) tanpa 404 dead-end, breadcrumb `<a href>` preserve right-click, `router.push` SPA, `onBeforeRouteLeave` unsaved guard — AC-D01.
- [ ] **Tiptap guard navigability** — klik `+ Buat Component` dedicated page → Tiptap init guard → no `configure` crash console → toolbar grouped visible → `+ Binding` 44px hit → insert pill → save → delete → 409 ReferenceList not Server Error — AC-D02.
- [ ] **Builder polish navigability** — drag `Kop` dari Library `NTree` → Canvas warm → Properties live → Looping pilih semua indeterminate → `EmptyStateCard` hilang saat ada blok → selection ring + hover `Edit/Delete` → Preview tabs — AC-D03.
- [ ] **Administrasi DR-002 navigability** — pick `Template SK` → rows per-field muncul → leave `letter.tanggal` empty → Save → field merah highlight + `NAlert` summary `missing: letter.tanggal (DR-002)` + scroll to first + tooltip → fill → success — AC-D04.
- [ ] **Component library relevance** — setiap halaman polish memakai library tepat (Tiptap office-min untuk surat, `NSteps` untuk wizard, `NTree` untuk Library, `StepMappingEditor` untuk DR-002) + documented trade-off vs Quill/Slate/MUI (Tiptap wins binding pill) — AC-D06.
- [ ] **Test flow + video design** — tabel `E2E-01..06` mapping 24 steps + ALT/ERR → E2E case `video: 'on'` dengan artifact `video.webm` terdokumentasi — AC-D07.
- [ ] **Responsive proof** — screenshot tablet drawer + mobile NTabs + toolbar scroll-x + 44px hit + dedicated pages 2-col→1-col — AC-D08 viewport 1280/768/375.
- [ ] **States & error hardening polish** — validation inline `NFormItem` + `DR-002` per-field + `NAlert` + 409 `ReferenceList` `data-testid=conflict-references` + 403 single `data-testid=access-denied` + Tiptap configure error + 500 PDF retry tanpa reset + 404 `NEmpty+Kembali` — story variant pass AC-D05.
- [ ] **State persistence polish** — visibility per slug `localStorage:master-data:pegawai:visibility` (min1 guard) + draft wizard `localStorage:letter-builder:draft:<slug>` + pagination ID `Menampilkan {from}-{to} dari {total}` + dedicated page draft autosave `localStorage:letter-builder:draft:component|template|administration` — AC-D01.
- [ ] **Handoff readiness** — `LetterBuilderPolish/*` 7+ stories + `wireframes/` 18 SVG + `mockups/` + `Test Plan` `video` mapping lengkap untuk FASE 2 `tasks/11-letter-builder-polish/` — AC-D08.

## Assumptions

- FASE 1 tidak menginstall runtime `puppeteer` Chrome baru atau `@vueuse/core`/`vue-draggable-plus` baru — hanya spec + Storybook mock (`withProviders` decorator mock `$fetch`/`useAuth` + Tiptap extensions mock guarded). Install & wiring di FASE 2 `11` tanpa ubah token visual — konsisten `OUT OF SCOPE` spec.
- **Tracer bullet**: Tiptap `^3.31.3` tetap terkunci Task07 + polish penambahan `extension-placeholder` `extension-highlight` `extension-color`/`text-style` `extension-character-count` untuk UX office-min placeholder + highlight `is_looping` node + BubbleMenu (`@tiptap/extension-bubble-menu` bila tidak ada `FloatingMenu`) diasumsikan additive minor tanpa migrate DB — FASE 2 install + 2 story variants (`TiptapToolbar` + `tiptapConfigureError`). Jika contoh `https://tiptap.dev/docs/examples` memerlukan `extension-typography` heavy (smart quotes) maka ditolak untuk `very minimum`.
- **Dedicated pages tidak breaking API**: `app/pages/dashboard/.../create.vue` + `.../[id]/edit.vue` hanya mengonsumsi endpoint existing `POST /api/doc-components` `PUT /api/doc-components/:id` etc. (reuse 05-07 DTO Zod). Tidak menambah `server/api` route baru; `NModal` lama di `components.vue:159` / `templates/index.vue:149` / `administrations.vue:163` tetap ada sebagai fallback tetapi bukan jalur utama — story `default` tetap pass via page route (design polish menyebut `modal hanya delete picker`, bukan create).
- **Permission stub**: `server/utils/permission-matrix.ts` tetap stub kosong (di-pin test Task02); FASE 1 tidak mengubah; FASE 2 tetap backfill seed `LetterBuilder:*` + guard URL allow `/api/master-data/*`, `/api/doc-*`, `/api/administrations/*`, `/api/documents/*` (pola Task05-07).
- **DRAG fallback**: HTML5 `draggable` native diasumsikan cukup untuk desktop builder; `@vueuse/core` helper `useDraggable/useDropZone` & `vue-draggable-plus` sebagai alternatif progresif — decision final di FASE 2 setelah A/B Storybook play (keduanya design-ready di Library rationale).
- **PDF**: Chrome Puppeteer diasumsikan tersedia di CI via `npx playwright install chromium` atau `PUPPETEER_SKIP_DOWNLOAD=0`; `ERR-11` design: `500 Failed to generate PDF` → `NAlert` + `Coba lagi` tanpa hapus draft; binary live diverifikasi via mock unit di Task05 — E2E `preview-pdf` step di-skip bila Chrome absen via `test.skip`, bukan fail (video artifact tetap record `retry` UI).
- **Playwright video tradeoff**: `video: 'on'` menghasilkan `video.webm` per-test bahkan pass (hemat disk 1280×720 ~1MB per 30s); `retain-on-failure` hemat tetapi `AC-D07` meminta "merekam video untuk setiap pengujian" → spec memilih `mode: 'on'` untuk `E2E-01..06` polish + `size 1280×720` (`https://playwright.dev/docs/videos#record-video` contoh `video: 'on'` + `videoSize`). Jika CI disk limit, fallback `retain-on-failure` + `trace: 'on-first-retry'` — keduanya design-ready; default `on` untuk 10.
- **Operasi-teks tokenizer reuse** 100% `server/services/expression.service.ts` 05 → preview klien reuse `evalTextOperation` identik by-construction — tidak duplikasi logic.
- **Sidebar dinamis**: Menu `Master Data → [display_name]` + `Persuratan → [Administrasi]` diasumsikan static grup + link dinamis via page-level fetch (tidak fetch di `AppLayout` — menjaga layout stabil, konsisten Task06/07).
- **Wireframes lokasi**: `tasks/10-letter-builder-polish-ui-design/wireframes/` mengikuti praktik 03 `tasks/03-redesign-ui-design/wireframes/` + 08 `tasks/08-letter-builder-ux-improvement-ui-design/wireframes/` — bukan `docs/wireframes/` (dihapus Task01) — FASE 2 reuse asset.
- **Nomenclature**: Istilah kanonis **Master Data** (Task06) + **Surat** untuk hasil administrasi — konsisten PRD §7-8 & `docs/database.md` meta `master_tables`.
- **Image storage**: kolom `image` reuse subfolder `general` via `POST /api/settings/upload` — tanpa `master/` baru; allowlist URL `https://`/`/api/storage/`/`data:image/` di renderer tetap (DR-003).
- **Tiptap `configure` root cause**: diasumsikan `await import('@tiptap/starter-kit')` ESM `default` interop kadang `undefined` bila `esModuleInterop false` + `StarterKit` import `* as StarterKit` → `.configure` di `undefined`. FASE 1 tidak run `vue-tsc` dengan `allowSyntheticDefaultImports` fix — itu FASE 2. Design guard documented di spec FR-005.

## Open Questions

- Apakah perlu **import Word/PDF → template** (`mammoth`/`pdfjs`) di roadmap berikutnya atau tetap manual Tiptap untuk MVP? Jika ya, toolbar perlu `Import Docx` button + `NUpload` dragger — tradeoff size 500KB.
- Apakah auto-number `document_number` (`SK/{year}/{seq}`) perlu generator di `master_tables:config_json` templated sequence atau tetap manual + saran format di wizard + `NTag` hint?
- Seberapa sering `is_looping` card bersarang 2 level dipakai (EmployeeCard: avatar+nama+NIP di dalam repeater trips)? Jika >20% pertimbangkan `component-ref` depth 2 dengan siklus guard — tradeoff perf vs fleksibilitas (EC-02)?
- Apakah master `office.logo/signer` lebih baik dipindah dari `Settings` key-value ke tabel `mst_office` khusus (agar bisa dipilih via relation picker) atau tetap `system` source (Task07 decision) untuk kesederhanaan? Polish mapping per-field sudah bisa pick `system:signer`.
- Butuh mode `strict` preview (`?strict=1` fail bila binding hilang) untuk QA atau cukup warning header (Task05 precedent)?
- Cap `100 kolom / 50k baris` (EC-06) perlu virtual scroll di definisi builder sekarang atau tunda hingga telemetry >50 kolom real?
- Apakah `NCode` preview HTML cukup atau perlu `prosemirror-view` live diff untuk binding (`before/after`) di Properties panel? `TiptapToolbar` story sudah show `CharacterCount`.
- **Video**: apakah `video: 'on'` per-test (`https://playwright.dev/docs/videos#record-video` `video: 'on'` ) atau `retain-on-failure` + `trace: 'on'` lebih hemat? Default spec `on` untuk AC-D07 tetapi CI artifact 6× `video.webm` ~6MB — apakah limit? Fallback artifact `retain-on-failure` untuk `CI=true`?
- **Dedicated page fallback**: apakah `NModal` lama harus dihapus total di FASE 2 atau dipertahankan sebagai fallback `showCreate = false` default false (hidden) untuk backward compat deep-link modal `?modal=1`? Design 10 memilih hidden (route utama), modal tidak render tanpa `showCreate`.
- **`configure` fix**: apakah cukup guard `if (!Mod?.configure)` fallback atau perlu pin `tsconfig.json:esModuleInterop: true` + `allowSyntheticDefaultImports` + `vite alias` untuk `@tiptap/*` ESM? Minimal 10 hanya guard; interop fix di 11 build.
- **DR-002 UX**: per-field editor butuh `debounce 300ms` search untuk `target` master_data atau cukup `NSelect filterable` static list 20 tables? Cost fetch `GET /api/master-data/:slug/schema` per-step vs cached?
- **Toolbar very minimum**: apakah `Highlight`/`Color` perlu atau cukup `Bold/Italic/Underline/Strike/Heading/List/Table/Align/Image/Link` sebagai `very minimum office`? Design 10 includes Highlight/Color tetapi bisa toggle via Storybook `args`.

## Related Knowledge

- `docs/PRD.md` §4-10 (RBAC + Goals) + §17.6 Table Browse + §18 Alur Authorization + §19-20 API/Routes + §22 Seed + §23 Client-Side Authorization.
- `docs/architecture.md` § RBAC Architecture (RBAC-Only 9 schemas/12 tables) + § Nuxt 4 Project Structure + § API Endpoints + § Table Browse Component (320/160 + Restart + error slot `NAlert` + Settings + `useDataTable`) + § PageShell (`head 16×20 body 24 radius 12` + hairline) + § Storybook Foundation + AppLayout sidebar 220/72 + § E2E Testing (headed default `SLOWMO_MS 100`, `HEADLESS`, `video retain-on-failure`, `reuseExistingServer`).
- `docs/database.md` § ERD + § Entity Details 1-12 + § Seed Data + § Relationships (9 EntitySchemas) + meta `master_tables/columns` & `doc_*` & `mst_*` + `DR-002` invariant `mapping_json` completeness.
- `docs/design-system.md` § Color Palette (Notion `#0075de/#0069c4/#005bab` + canvas `#f6f5f4/#ffffff`, hairline `#e6e6e6`, Ink, Sticker) + Typography Inter tracking + Spacing xxs4-xxl32 + Radius xs4/full + Elevation Level1/2 + Table + System Logs + PageShell + Responsive + Icons `@vicons/carbon` h-render + Animations `prefers-reduced-motion` + Chrome Patterns (Badge Pill, Empty-State Card, Toast, Auth Card, Modal Card, App-Shell Row) + Foundation Deliverables.
- `AGENTS.md` (EntitySchema, plain-object service, Zod DTO, Nitro `defineEventHandler` + `QuerySchema` + `createError`, `useApi` 401/403, Tailwind v4 no-preflight `h(NIcon)`).
- `tasks/05-document-engine/*`, `tasks/06-master-data-ddl/*`, `tasks/07-template-administration/*` (engine + DDL 13 tipe + Tiptap + Builder 3-pane + Wizard NSteps + seeder demo).
- `tasks/08-letter-builder-ux-improvement-ui-design/*` + `tasks/09-letter-builder-ux-improvement/*` (baseline polish 08 wireframes 8 SVG + stories 9 files + 09 409 hardening + E2E 6 files) — polish ini merevisi 08 layouts ke dedicated pages + Tiptap guard + mapping per-field.
- `tasks/03-redesign-ui-design/*` + `tasks/04-redesign/*` (Notion-calm baseline 11 halaman + stories `redesign/` 8).
- `app/pages/dashboard/components.vue:1-198` (modal create, `ComponentEditor` import), `app/pages/dashboard/templates/index.vue:1-184`, `app/pages/dashboard/templates/[id].vue:1-121` (builder grid 260|1fr|320 minimal), `app/pages/dashboard/administrations.vue:1-195` (NDynamicInput mappingText textarea JSON + DR-002 400 generic), `app/components/features/persuratan/ComponentEditor.vue:1-167` (Tiptap import + `configure` + `BubbleMenu` contextmenu + `DocBinding` nodes), `app/utils/tiptap-nodes.ts:1-67`, `playwright.config.ts:1-32` (headed + `video: 'retain-on-failure'`), `server/dto/persuratan.dto.ts` (DR-002), `server/services/administrations.service.ts` (`replaceSteps` DR-002), `server/services/doc-components.service.ts` (findReferences).
- `https://tiptap.dev/docs/examples` — referensi toolbar office-min (StarterKit, Typography, Placeholder, TextAlign, Highlight, Color, Link, Image, Table, BubbleMenu, FloatingMenu, CharacterCount).
- `https://playwright.dev/docs/videos#record-video` — referensi video recording (`video`, `trace`, `size`, `mode: 'on' | 'retain-on-failure' | 'off'`, `contextOptions`, `testInfo.attach`).
- `app/utils/naiveui-theme.ts` live token + `app/assets/css/main.css` (`tailwindcss/theme` + `utilities`, no preflight, `:root` token, `prefers-reduced-motion`) + `apps/web/.storybook/{main.ts,preview.ts}` (stories `../stories/**/*.stories.*`, addons `a11y`+`docs`, `vue3-vite`) + `stories/letter-builder/*` 08 + `stories/foundation/*` 3 files + `docs/wireframes` dihapus Task01.
- `.ua/` knowledge graph 754 nodes.

## Change Log

### Initial — FASE 1 UI-First Polish (Dedicated Pages + Tiptap Hardening + Builder & Mapping Redesign + Video)

- Task specification created (FASE 1 — UI Design: Wireframe/Mockup/Prototype + Playwright video + Tiptap office-minimum). Sumber: user prompt 2026-09-14 — `dedicated create/update halaman baru layout sama; fix Tiptap Cannot read properties of undefined (reading 'configure') create & delete; fix ui /dashboard/templates/2 Builder lebih cantik & mudah; fix DR-002 Form Administrasi Mapping incomplete missing: field lebih mudah dipahami; perbaiki semua ui/ux mudah digunakan; Tiptap https://tiptap.dev/docs/examples toolbar office very minimum; Playwright https://playwright.dev/docs/videos#record-video record video` — ditafsir sebagai polish Letter Builder lanjutan 08/09 dengan UI-First murni (dedicated pages + Tiptap guard + Builder & mapping per-field + video config). Deliverables: `wireframes/` 18 SVG (desktop/tablet/mobile + builder-3pane + dedicated create/edit ×4 + tiptap-toolbar/tiptap-error + states/validation/conflict) + mockup Vue Tailwind + Storybook `apps/web/stories/letter-builder-polish/` 7 stories × variants (dedicated pages, Tiptap toolbar office-min, Builder polish, Administration mapping DR-002, States) — library Tiptap 3.31.3 office-min (StarterKit + Placeholder + Highlight/Color + TextAlign + Link/Image/Table + BubbleMenu/FloatingMenu) + Naive UI 2.44 + Tailwind v4 + `@vicons/carbon` h-render + `@vueuse/core` helper + Playwright video `on` 1280×720 trace. Next: `tasks/11-letter-builder-polish/` FASE 2 Implementation (routing + Tiptap guard wiring + builder & mapping per-field + video `playwright.config.ts` hardening + `npm run test:e2e` video artifacts) setelah Status DONE + `vue-tsc 0` + `build` + `build-storybook` PASS.

